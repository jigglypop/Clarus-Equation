# Confirmation estimand lock

Status: COMPLETE

The contraction theorem and paired estimands are inherited without modification from the
development run. Bootstrap units remain seed rows. The primary difference and factorial
interaction are not redefined after seeing confirmation data.
