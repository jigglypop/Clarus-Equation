# Routes

Status: COMPLETE

Chosen route: the exact direct product of two frozen 20-dimensional BA-TR4 payload runtimes, with two count-normalized local factor gates. This preserves the verified threshold, delay, STP, recurrent path, decoder, and zero-store semantics without introducing a new relay or recalibrating a 40-dimensional runtime.

Rejected for this iteration:

- One new 40-dimensional runtime: mathematically equivalent only after duplicating every threshold/profile/detail and creates an unnecessary apparatus change.
- A shared output block: permits cross-factor endpoint leakage.
- Raw Hebbian accumulation: confounded by the proved marginal-frequency obstruction.
- A default action for the unseen joint lookup: succeeds by chance on one quarter of balanced mappings; the correct control is abstention before endpoint.
- Reward or decoder-gradient training: changes the question from local experience composition to answer-supervised selection.

If this route passes, the next falsifier must remove the declared factor/support decomposition or introduce controlled interactions. If it fails, retain the failure and do not tune the held-out pair, counts, thresholds, decoder, or static controls.
