# Routes

Status: COMPLETE

## R1 — frozen-p randomized property witness (selected)

- eight frozen seeds and alternating `(q,D,T)` from `00-contract.md`.
- nonlinear rollout versus analytic tangent.
- total circuit derivative versus epsilon central difference.
- consistent delay-lifted chart covariance.
- augmented-state Gramian, weighted least-norm identity, reachability controls.
- state-dependent efficacy full derivative and omitted-term adverse fixture.
- claim ceiling: `MATH_PROPERTY_PASS / EMPIRICAL_UNTESTED`.

R1 is selected because it attacks every equation used by A6-P/C without opening empirical
endpoints or adding BrainRuntime's hybrid semantics.

## R2 — one formula revision (conditional)

R1에서 `P0_FORMULA`가 발생할 때만 연다. analytic indexing 또는 chain rule만
고치며 seed, finite-difference step, tolerance, dimensions, ranges와 killing controls는
그대로 둔다. 같은 gate 재실패는 `STOP_MATH_PROPERTY`다.

## R3 — hybrid/saltation runtime bridge (deferred successor)

actual BrainRuntime의 active mask, hard threshold, clamp, STP, refractory/adaptation,
delay buffer와 backend 차이를 포함하는 별도 augmented hybrid system이다. switching
surface, one-sided derivative 또는 saltation matrix, Torch/Rust semantic parity receipt를
먼저 고정해야 한다. R1의 성공을 R3의 성공으로 읽지 않는다.

## R4 — cortical morphology bridge (blocked)

longitudinal cortical embedding, thickness, material/growth law, boundary condition,
cell/circuit-to-tissue observation map이 필요하다. 현재 `BLOCKED_INPUT`이며 synthetic
state-space metric으로 대체하지 않는다.

## 순서

1. R1 apparatus를 source-only review한다.
2. R1을 한 번 실행하고 machine-readable receipt를 동결한다.
3. failure를 `P0_FORMULA`, `P1_DOMAIN_BOUNDARY`, `P2_APPARATUS`로 분류한다.
4. P0일 때만 R2를 한 번 실행한다. P1/P2는 claim 또는 apparatus를 정직하게
   좁히며 식을 성능 맞춤형으로 바꾸지 않는다.
5. 독립 status audit 뒤 predecessor ledger/canonical 문서에 필요한 경계만 반영한다.
