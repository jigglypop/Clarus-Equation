# Cortical–basal-ganglia rewrite contract

The previous Loop 8H–8L results remain immutable checkpoints and counterexamples.
This run does not erase them or rename their numerical results. It replaces the
mechanistic interpretation and derives a new candidate before implementation.

The candidate must satisfy all of the following:

1. latent-state inference and action selection are separately identifiable;
2. a global STN term has a non-zero mathematical effect;
3. reconvergent DAG routing conserves probability mass;
4. multi-path credit is not multiplied by the number of paths;
5. every within-tick computation terminates in finite time;
6. every exponential, logarithm, sigmoid, and probability argument is
   dimensionless;
7. tree, shared-DAG, factorized-flat, and topology-destroyed controls can falsify
   the claimed source of any gain.

No claim of anatomical identity is authorized. The anatomical CBGTC circuit is
recurrent; the DAG is a finite option-routing abstraction inside one tick.
