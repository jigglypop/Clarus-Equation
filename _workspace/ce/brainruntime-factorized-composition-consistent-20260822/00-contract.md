# BA-TR19 decoder-consistent factorized composition contract

Date: 2026-08-22

BA-TR18 remains frozen as a control-gate STOP. BA-TR19 uses calibration seed
`106001` and development seeds `106101..106116`, all fresh. Atomic experience,
source-factorized routing, cyclic target map, delay, weights, and horizon are
unchanged.

The sole apparatus correction is to use the already frozen native atomic
decoder threshold `1e-5` for every atomic, simultaneous, and independent-union
component decision. This value predates BA-TR18 and is not selected from its
outputs. No target or decoder value enters learning or routing.

The factorized equation remains

\[
c_h=\sum_{s\in A_t}
\left[[W_{hs}p_s]_+-\max_{k\ne h}[W_{ks}p_s]_+\right]_+.
\]

The probe additionally records the number of declared source coordinates in
every delayed packet. Because BrainRuntime activations persist, the current cue
generates a short packet stream; this test must not be described as a
single-spike result.

GO requires all 16 rows: singleton parity and 64/64 atomic recall; factorized
64/64 exact two-target recall; legacy global WTA 0/64; misaligned source
receipt 0/64; independent union 64/64; all first-arrival packet counts exactly
two for pair probes; later repeated-packet receipt reported; stores zero.
Global adaptive top-2 remains descriptive. Confirmation stays sealed.

Claim ceiling: deterministic synthetic composition from explicit delayed
source-packet provenance under a persistent packet stream. No support/module
discovery, biological spike claim, curvature, or AGI.

