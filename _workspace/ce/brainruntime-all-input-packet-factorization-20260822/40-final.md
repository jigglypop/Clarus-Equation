# BA-TR21 final

Status: `SYNTHETIC_ALL_INPUT_PACKET_FACTORIZATION /
DEVELOPMENT_GO / CONFIRMATION_SEALED`.

The explicit source projector was unnecessary on 16 fresh seeds. Treating all
arriving presynaptic coordinates as candidate packet identities reproduced the
projected route bit-for-bit and recovered all 64 two-item compositions. Merely
shifting the learned weight columns while retaining packet coordinates reduced
the result to 0/64, identifying the local (j\leftrightarrow W_{:j})
correspondence as the operative information.

This is not yet support discovery. The learned circuit is sparse: only the
four source columns have nonzero support into H, so (W) itself filters other
coordinates. The next falsifier must add outcome-blind nonzero distractor
columns with matched scale and ask whether a local rule can reject them without
restoring a hand-declared source mask.

Claim ceiling: synthetic composition from all actual delayed packet
coordinates in a sparse learned circuit. No robustness to dense connectivity,
biological synaptic tags, cortical geometry, or AGI is established.

