# BA-TR25 final

Status: `SYNTHETIC_CONDITIONAL_RANK2_CONTENT_TRANSFER /
DEVELOPMENT_GO / CONFIRMATION_SEALED`.

BA-TR25 removes BA-TR24's separate factor-code tables and fixed source-index
compiler.  Three raw mixed-cue episodes determine a two-dimensional affine
contrast plane, and the held-out fourth cue is reconstructed by

$$
\widehat y(r)=y_0+D(R^\top R)^{-1}R^\top(r-r_0).
$$

The predicted content is bound to current packet coordinates by comparing all
arrived packet-pair content sums.  All learned packet columns were moved to an
unseen input block before recall.  The learned rule matched the remapped oracle
on all 16 fresh seeds, while every lookup, coordinate, cue, binding, rank-one,
and no-context control failed.

This does not establish unconstrained factor discovery.  The same three
training observations admit arbitrary nonadditive fourth masks; success is
conditional on the predeclared cue parallelogram and additive content law.
The current weight column also supplies a role-neutral packet identity needed
to resolve a new physical coordinate.  Thus the admitted result is a
synthetic rank-two additive subspace with content-equivariant coordinate
transfer, not a biological memory, autonomous semantic factorization,
curvature mechanism, cortical-folding mechanism, or AGI result.

Next falsifier: BA-TR26 must use a richer `3 x 3` mixed-cue table, verify
multiple training rectangles before the held-out cell, and learn a shared
content gate across fresh episode-wise coordinate remaps.  It must beat joint
lookup and coordinate memorization without receiving factor names or a fixed
packet-role table.

