# BA-TR27 final

Status: `SYNTHETIC_CONDITIONAL_NONZERO_TWIST_EQUIVALENCE_TRANSFER /
DEVELOPMENT_GO / CONFIRMATION_SEALED`.

BA-TR27 made BA-TR26's additive explanation false on the eight observed rows.
The fitted nonzero-twist family reconstructed those rows at machine precision,
whereas the same-capacity additive arm retained at least `0.4202` relative
residual.  It then predicted the missing content sum, bound it to columns that
had moved to a disjoint input block, and matched both oracle and atomic-union
runtime targets on all 16 fresh seeds.  Every declared destructive control
failed.

The result is narrower than an exact $\kappa$ discovery.  Both nonzero labels
remain in one chart/gauge orbit, and all seeds only permute one fixed positive
content template.  More fundamentally, a query-only $\delta$ produces the
same eight observations.  The admitted result is therefore conditional
identification of a nonzero-twist equivalence class and its query prediction
inside a predeclared finite family.  It is not arbitrary interaction or factor
discovery, a biological mechanism, curvature/folding evidence, physical
energy evidence, or AGI evidence.

Next falsifier: BA-TR28 must remove the hand-enumerated three-class family.  A
generic interaction learner should infer a sparse bilinear or low-degree
operator from multiple missing-cell rotations, beat affine and finite-table
controls on fresh held-out cells, and fail closed on an explicit query-only
interaction that remains outside its declared function class.
