# BA-TR15 final

Status: `SYNTHETIC_LOCAL_ATTENUATION_COMPENSATED_BINDING /
DEVELOPMENT_GO / CONFIRMATION_SEALED`.

The fixed local packet compensation rescued the exact failure class exposed by
BA-TR14. On fresh source codes, the unchanged write passed only 14 of 16
absolute recall gates, while the compensated local rule passed 16 of 16 and
raised the worst cue-only target margin from `2.49e-6` to `3.99e-5`. No decoder
threshold, horizon, source-code producer, or runtime dynamics were retuned.

The causal interpretation is narrow. A first-arrival hidden event identifies
which local synapse is eligible; the actual terminal packet determines how
strongly that synapse must be written, within a finite cap. This makes a later
source cue re-create the experienced target after all auxiliary stores are
removed. Packet amplitude alone does not discover identity, and the result is
not evidence for curvature as memory.

Confirmation remains sealed. The next useful falsifier is transfer beyond the
declared one-to-one `S/H/Y` block scaffold: reuse the same local rule in a
many-to-one or compositional association where output identity cannot be
recovered from fixed coordinate correspondence.

Claim ceiling: synthetic, experience-supervised, bounded local attenuation
compensation for delayed association. No semantic, biological, anatomical,
curvature, morphology, or AGI claim.

