# Status audit

Status: COMPLETE

Gate: PASS

The contract and all lane statuses satisfy the preimplementation gate. Missing executable
results are expected at this stage and remain obligations of the build and final gates.

## Formal status

- The authorized claim is a repository-native computational experiment only.
- Native-runtime criteria, Loop 6--10 thresholds, controls, and confirmation seeds are fixed.
- Route A is selected. Route B remains a separately labeled fallback after Route A freezes;
  Route C cannot satisfy the contract.
- Biological memory, consciousness, AGI, and SCC-equivalence claims are out of scope.

## Mandatory build gates

1. Add an opt-in causal eligibility orientation and prove asymmetric
   $\Delta W_{ij}>0$ while preserving the legacy default.
2. Detach both temporal episodic and hippocampal stores before recall.
3. Decode only a fixed codebook from cue plus zero-input free rollout.
4. Use matched initial weights, seeds, timing, and paired no-replay and target-shuffled controls.
5. Use Torch CPU, zero noise, no axon delay, and a deterministic reset policy.
6. Use held-out factorized intervention combinations for Loop 9.
7. Limit Loop 10 to bounded self-prediction and error monitoring with no post-state leakage.

The first audit and its stage-semantics correction are preserved in
`revisions/20-audit-v1.md` and `revisions/log`.
