# Frozen geometry-route map

Status: COMPLETE

| Route | Intervention | Strongest counterexample | Maximum claim |
|---|---|---|---|
| G1 | precommitted `S-to-T` recurrent edge block | matched `S-to-N`, gain, noise, sham | joint runtime weight effect on SPD summary and dynamics |
| G2 | fixed W, gain/noise factorial | raw `B/C` and unrestricted direct dynamics | compressed operational utility only |
| G3-D | confirmed M1 contingency + independent probes | shuffle, no replay, weight permutation, summary-null lesion | non-mediational response/recall co-change diagnostic only |

Implementation order is G1 invariant tests and development, then G2 feature-budget amendment, then
G3 with the frozen procedure. A G1 failure stops validated estimator inheritance but does not erase
its algebraic tests. A G2 failure narrows the result to an SPD response summary rather than a unique
geometric law. No route can inherit the descriptive PFC covariance result or the supervised M0
ceiling.

Development status: G1 is `STOP` (0/16 circuit-level GO) despite positive average endpoint and
cross-response contrasts. G2 is also `STOP` (0/16): horizon-matched raw `B_h`, direct quadratic,
and termwise-C baselines beat the compressed metric feature. G3-D was therefore executed only as an
independently audited diagnostic. Its valid replacement development run is `STOP`: matched M1 has a
large continuous-recall advantage, but its task-independent SPD response change does not reliably
exceed target-shuffle/structural controls or co-vary positively with recall. Original learned
mediation remains permanently blocked.
