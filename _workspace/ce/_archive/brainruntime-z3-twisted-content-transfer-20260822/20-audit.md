# BA-TR27 audit

Status: `PASS`.

Independent stable-snapshot audits found no mathematical or implementation
P0/P1 blocker.  Contract, code, tests, and artifacts agree on the 72-chart
apparatus enumeration, rank-five incidence/query gate, pseudoinverse fit,
additive separation, gauge agreement, current-column binding, one-shot delay,
and endpoint metadata.

The generic learner receives only raw cue and observed content matrices plus
the raw query cue.  The compiler receives only a gate, cue, arrived
coordinates, current weight columns, and response coordinates.  Seed parity,
the actual twist, cell coordinates, targets, decoder values, rewards, stores,
and query content remain in the synthetic harness and outside these APIs.

The claim wording is corrected to a nonzero-twist equivalence class: every row
admits both labels `1` and `2` under chart gauge, although their query
prediction is identical.  The 72-chart count and fixed positive content
template are apparatus-specific and must not be presented as broad
generalization.

`artifacts/source-freeze.json` binds twelve declared source, contract, input,
output, dependency, hook, and environment entries.  Local SHA-256
recomputation found no mismatches.
