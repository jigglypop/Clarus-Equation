# Gate audit

Status: COMPLETE

Gate: PASS

The new mechanism addresses BA-TR8's exact open dependency: the Boolean
occupied vector and hard allocator no longer enter the transition. The first
winner still requires the already admitted outcome-blind microscopic seed;
uniform weights remain an explicit symmetry no-go. Competition touches only a
real delayed recurrent packet, so it cannot create pre-arrival hidden activity.
The packet-tail envelope and no-reset washout rule close the repeated-emission
confound, while the source-independent-bias arm prevents a positional bijection
from being misreported as source identification.

All variables are dimensionless, `exp` receives a dimensionless argument, and
the output/decoder endpoint stays closed. Implementation is authorized only for
the frozen tuple and controls in `00-contract.md`.
