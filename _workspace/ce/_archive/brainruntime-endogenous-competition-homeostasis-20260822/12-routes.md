# Routes

Status: COMPLETE

Implement the default-off local competition state in `BrainRuntime`, including
Torch execution, reset, snapshot/restore, and Rust fail-closed behavior. Add one
isolated BA-TR9 module, one benchmark, and one focused test file. Do not alter
BA-TR8 or its artifacts.

First run only calibration seed `97091`. It is a pass/fail apparatus witness,
not a parameter search. If it passes, freeze source and calibration receipts,
then run development seeds `98201..98216`. Confirmation remains inaccessible.

The decisive comparisons are seeded persistent homeostasis, seeded
$\lambda=0$, uniform, source-independent row bias, hidden-row permutation, and
midpoint snapshot continuation. A negative development result closes this
equation; it does not authorize threshold, coefficient, delay, washout, or seed
tuning.
