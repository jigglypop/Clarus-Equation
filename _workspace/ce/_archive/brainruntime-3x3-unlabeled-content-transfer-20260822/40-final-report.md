# BA-TR26 final

Status: COMPLETE

Verdict: `SYNTHETIC_CONDITIONAL_RANK4_AFFINE_CONTENT_TRANSFER /
DEVELOPMENT_GO / CONFIRMATION_SEALED`.

BA-TR26 replaced the supplied two-factor compiler with a generic eight-row
SVD map.  It detected five raw-cue parallelograms, fitted one shared rank-four
cue-to-content relation, predicted the missing ninth content sum, and bound it
to packet columns that had all moved to a previously unused physical input
block.  On 16 fresh seeds, the learned route matched both the remapped oracle
and the union of current-snapshot atomic responses; all declared controls
failed.

This is a conditional synthetic result.  The seeds permute one fixed content
template, and the unobserved ninth value can always be changed by an arbitrary
$\delta$ without altering the eight training rows.  Therefore the admitted
claim is only conditional rank-four affine cue/content transfer with
current-column coordinate binding.  It is not factor discovery, a biological
memory or routing mechanism, curvature or cortical-folding evidence, physical
energy evidence, or AGI evidence.

Next falsifier: BA-TR27 should make the additive/affine model fail on observed
rows and predeclare a nonseparable composition family, for example

$$
y_{ab}=A_a+B_{(b+\kappa a)\bmod3},\qquad \kappa\in\mathbb Z_3\setminus\{0\}.
$$

It must select the interaction class from the eight observed rows, beat the
$\kappa=0$ additive arm under the same 30D runtime and current-column binding,
and retain the explicit $y_{22}+\delta$ no-go ceiling.
