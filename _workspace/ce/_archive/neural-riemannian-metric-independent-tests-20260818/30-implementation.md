# Implementation disposition

Status: SKIPPED (route-design run; no new eligible empirical input)

The user asked for alternative ways to test the current theory, not for a new
retrospective fit to E17. No runner, candidate tournament, or scientific result
was changed. Reusing E17 would repeat the same activity-derived information and
would not create an independent observation of geometry.

Two supporting artifacts were added within this run:

- `artifacts/check_counterexamples.py` contains deterministic spot checks for
  the rank-one sign, distance-versus-hitting-time, non-unique $W\to g$ maps,
  flat polar-coordinate curvature cancellation, and `S4(H=1)=S3` identity.
- `artifacts/capability-matrix.csv` records scale, identity, structure,
  activity, perturbation, longitudinal support, claim tier, and full-chain
  status for candidate datasets and technologies.

A future empirical implementation is eligible only after an experiment or
dataset supplies the inputs of a declared route in `12-routes.md`. Missing
structural, intervention, or longitudinal fields must remain missing rather
than being replaced with an activity-derived proxy.
