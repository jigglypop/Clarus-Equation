# BA-TR19 final

Status: COMPLETE

Verdict: `SYNTHETIC_FACTORIZED_PACKET_STREAM_COMPOSITION /
DEVELOPMENT_GO / CONFIRMATION_SEALED`.

With a consistent `1e-5` component floor, the unchanged source-factorized
competition rule recovered all 64 unseen two-source target unions on 16 fresh
development seeds. The legacy global WTA and the one-tick-misaligned source
receipt both recovered 0/64. Thus the BA-TR18 mechanism failure was not real;
its frozen STOP came from an inconsistent auxiliary control threshold.

The computational result is the noncommutativity

\[
\operatorname{Select}\!\left(\sum_s Wp_s\right)
\ne
\sum_s \operatorname{Select}_s(Wp_s),
\]

and the observed task requires the right-hand, source-provenance-preserving
operation.

The result remains narrower than a single-event routing claim. Every pair had
the exact receipt `[0,0,0,2,2,2,2]`: the same two source coordinates kept
emitting from first arrival through the readout horizon. The next falsifier
must preserve the learned weights and routing law while allowing exactly one
source packet to enter the delay ring.

Claim ceiling: deterministic synthetic source-factorized composition under a
persistent packet stream over declared source coordinates. No learned source
module discovery, biological spiking, cortical geometry, curvature memory, or
AGI conclusion follows.

