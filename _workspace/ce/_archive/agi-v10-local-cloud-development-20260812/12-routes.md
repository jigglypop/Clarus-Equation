# Route lock

Status: COMPLETE

Only one route is allowed: frozen V10 kernel, frozen factorial task, fixed ridge lambda, and
fresh development seeds. No gain, horizon, noise, context matrix, feature, threshold, or
comparator may change after registration. A STOP is preserved as the result; it is not followed
by same-run tuning. Confirmation is a separate future run and opens only after development GO.
