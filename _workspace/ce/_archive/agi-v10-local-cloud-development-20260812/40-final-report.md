# V10 local/cloud registered development result

Status: COMPLETE

## Outcome

The one-shot registered development gate is `GO`. The full local/shared transition achieved
`0.653015` mean accuracy against `0.508484` for the strongest aggregate factorial control. The
conservative seed-paired improvement over each seed's strongest control was `0.128479`, with
95% bootstrap interval `[0.119200, 0.137329]`.

The result is causally consistent within this synthetic mechanism test: the factorial interaction
was `0.148865` with positive interval, and cross-cut, local reset, and cloud reset each produced a
positive paired loss under the unchanged full readout. All integrity counters were zero.

## Claim boundary

- Supported: on the registered synthetic local-identity/shared-context task, this frozen
  recurrent transition and readout show a reproducible development increment over the registered
  local-only, cloud-only, and no-memory controls.
- Not supported: general intelligence, natural-language reasoning, long-horizon autonomy,
  biological CloudCell identity, whole-brain nested SCC identity, or superiority over arbitrary
  learned recurrent architectures.
- Confirmation: not yet run. The 64 reserved confirmation seeds remain unopened and require a
  separate run contract with no code or threshold change.

The old weak repeated-shell STOP remains a valid negative result for that old architecture. It is
not silently replaced; V10 is a different, explicitly interaction-bearing route.
