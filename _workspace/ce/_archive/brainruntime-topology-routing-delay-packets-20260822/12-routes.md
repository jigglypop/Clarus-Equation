# Routes

Status: COMPLETE

The route family and decision rules are inherited without change from
`brainruntime-topology-aware-routing-20260822/00-contract.md`: `FULL`,
`WEIGHT`, `CLUSTER`, `PATH_ONLY`, `TOPOLOGY`, `RETURN_SHUFFLED`,
`RANDOM_MATCHED`, and `WRONG_CONTEXT`. `PATH_ONLY` and `RETURN_SHUFFLED` remain
mandatory before any topology-specific interpretation.
