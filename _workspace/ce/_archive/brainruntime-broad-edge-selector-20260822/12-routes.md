# Routes

Status: COMPLETE

Chosen route: two independent shared-hidden broad substrates with a 32-by-2 count-normalized edge field and a pure top-four compiler.

Rejected routes:

- sparse BA-TR3 weights: the zero pattern leaks the answer;
- two hidden branch families: different supports can produce the same decoded payload and weaken support identifiability;
- weight-aware compiler: equal weights make it unnecessary and access would reopen the leakage path;
- lexicographic tie breaking: coordinate order becomes a hidden router;
- raw pooled eligibility: confounded by the proved 8:4 exposure imbalance.

The next route after a pass must remove the declared $S/H/Y$ block scaffold or remove the externally supplied $H$ coactivation demonstration.
