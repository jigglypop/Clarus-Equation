# Routes

Status: COMPLETE

The eight route definitions and path/return ablations are unchanged. Only the
common sparse budget is replaced by the outcome-blind feasible formula in
`00-contract.md`. The original path and topology-specific development gates
remain unchanged.
