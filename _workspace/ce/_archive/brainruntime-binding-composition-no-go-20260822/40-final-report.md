# BA-TR16 final

Status: COMPLETE

Verdict: `GLOBAL_WTA_COMPOSITION_NO_GO_CONFIRMED / DEVELOPMENT_16_OF_16 /
CONFIRMATION_SEALED`.

The learned memory contains both atomic routes, but the current router deletes
one whenever two are requested simultaneously. The obstruction is the global
competition equation itself:

\[
c_h=[v_h-\max_{k\ne h}v_k]_+,
\]

which admits at most one positive component. BA-TR16 therefore closes the
current global-WTA route for compositional recall.

The next candidate is an input-count-adaptive or genuinely module-local
competition rule. It must reduce exactly to the existing one-winner rule for a
single source and permit two winners for two concurrent source events without
receiving target identity or decoder feedback.

Claim ceiling: a synthetic routing-capacity no-go. It does not by itself
establish the biological implementation of modular competition.

