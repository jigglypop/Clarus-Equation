# BA-TR16 global-WTA composition no-go contract

Date: 2026-08-22

Fresh BA-TR10 source-code seeds are calibration `103001` and development
`103101..103116`. Atomic experiences use the frozen BA-TR15 local compensation
but a cyclic, nonidentity target map `pi=(1,2,3,0)`.

After the four atomic associations are frozen and all auxiliary stores are
removed, the never-coexperienced cue pairs are

\[
(0,2),(0,3),(1,2),(1,3),
\]

with expected two-component output set `{pi(i),pi(j)}`. Both expected target
activations must exceed `2e-5`; no other target may do so.

The existing hidden competition is

\[
c_h=[v_h-\max_{k\ne h}v_k]_+.
\]

For any finite vector `v`, at most one component of `c` is strictly positive.
BA-TR16 therefore asks whether the actual runtime exhibits the predicted
simultaneous-composition failure while each atom remains independently
recallable. An independent-union counterfactual probes the two sources from
separate clones and takes the union of their outputs; it must recover both
components. It is a capability witness, not an equal-cost route.

The no-go is confirmed only if every fresh source code has 4/4 atomic recall,
0/4 simultaneous composition, at most one positive first-arrival hidden unit
for every pair, and 4/4 independent-union recovery. No thresholds, gains,
weights, or horizons may be changed after calibration.

Claim ceiling: global max-relative WTA in this synthetic runtime is
incompatible with simultaneous two-route memory composition. This is not a
biological, anatomical, curvature, or AGI claim.

