# Routes and falsifiers

Status: COMPLETE

The implementation route is Revision 1's five-block shared-output-trunk
construction in `00-contract.md`.  Revision 0's dead extra relay is retained
as the first failed formula witness.  The matched falsifiers are `WRONG`, `STATIC_0`, `STATIC_1`,
and `RANDOM_MATCHED`; `FULL`/`STATIC_UNION` is the larger-support interference
control.  The direct intervention swaps only the context mask after the cue.

There is no threshold, decoder, horizon, budget, seed, or topology-score
alternative on this development cohort.  If the dynamic branch fails, the
result is `CONTEXT_BRANCH_NOT_IDENTIFIED`.  If it passes, general motif or
morphology routing remains a separate future question.  No further formula
revision is authorized on these development seeds.
