# Mathematics

Status: COMPLETE

For each source column, $\sum_h\xi_{hs}=0$ and $\|\xi_{:s}\|_2=1$. With $\epsilon=0.2$ and $\|\xi\|_\infty=3/\sqrt{20}$,

$$
1-0.2\frac{3}{\sqrt{20}}>0,
$$

so every candidate weight is positive. Componentwise `tanh` is strictly increasing, hence the four distinct first-arrival drives give a unique local ordering before threshold, bit, or lifecycle updates.

The local evidence satisfies $z_{hs}>0$ and $\sum_h z_{hs}<1$ because of the positive stabilizer $\varepsilon_0$. If $r_h=1$ then $u_{hs}<0.0$ for $\lambda_r=1.1$, while every unused coordinate has $u_{ks}=z_{ks}>0$. Thus the first four distinct source presentations allocate four distinct hidden coordinates, provided each source-local winner itself has a positive margin. Uniform $B=1$ has no first winner and must abstain before this induction starts.

The allocation is not an output identity theorem. Any permutation of an unobserved hidden-to-output code leaves all source-only observations invariant while changing the semantic output correspondence.

## Dimensionless audit

| quantity | dimension vector | status |
|---|---:|---|
| normalized activation, $E,z,B,\xi,u,L,r$ | $(0,0,0,0)$ | dimensionless |
| $\epsilon,\varepsilon_0,\lambda_r$ | $(0,0,0,0)$ | dimensionless |
| tick, support count, seed | unitless index/count | not a physical time or energy |

The normalization denominator and `argmax` inputs are dimensionally consistent. This check does not establish biological meaning.
