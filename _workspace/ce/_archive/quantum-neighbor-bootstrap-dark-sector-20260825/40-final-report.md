# Final report: quantum-neighbor bootstrap and residual dark sector

Status: COMPLETE

## Result

The user's two ideas can be joined only as a three-stage conditional chain:

1. **Neighbor facilitation.** The declared local jump
   `L_{i<-j}=sqrt(kappa_ij) sigma_i^+ n_j` makes active quantum node `j` a
   necessary gate for activating neighboring node `i`. In an
   occupation-diagonal sector this gives an exact classical population
   process. The neighbor does not supply energy; a seed and bath/drive energy
   ledger are required.
2. **Bootstrap limit.** Mutual graph reachability is an SCC property, but an
   SCC is not a survival theorem. Every finite declared network with positive
   decay absorbs in the vacuum almost surely. Nonzero infinite genealogy is a
   theorem only after an independent branching limit with fresh targets,
   independent clocks, negligible collisions/correlations, and a
   seed-reachable class satisfying `rho(A)>1`.
3. **Residual dark-sector map.** A monitored instrument may define selected
   and nonselected records, but standard conditional quantum mechanics does
   not add the nonselected outcome as a gravitational source in the selected
   branch. CE may introduce a local-covariant map from nonselected history data
   to a residual field as new physics. Given the minimal scalar action, rapid
   quadratic oscillations are DM-like (`<rho> proportional to a^-3`) and a
   constant offset is DE-like (`w=-1`). Their common nonselected-history origin
   is therefore an explicit physical-map axiom followed by conditional EFT
   theorems, not a result of standard quantum theory.

## Exact and incomplete claims

| Layer | Status |
|---|---|
| diagonal-sector CTMC and correlation hierarchy | theorem, conditional on the declared jumps and diagonal/decohered Hamiltonian sector |
| finite vacuum absorption; SCC not sufficient | theorem/no-go |
| independent Poisson offspring and Perron survival | conditional theorem inside the separately declared branching limit |
| uniform scalar fixed point and Lambert-W root | exact inside that Poisson model |
| `D_eff=d+delta` | CE readout axiom; microscopic derivation incomplete |
| selected/nonselected instrument | additional structure; microscopic derivation incomplete |
| nonselected histories to residual stress | CE physical-map axiom; locality, covariance, matching and no-double-counting incomplete |
| oscillatory residual to DM-like and constant residual to DE-like | conditional EFT theorems |
| absolute `Omega_DM`, `Omega_Lambda`, or their split | not identified or predicted |

The numerical survival value `0.9513532804` is a genealogy probability for the
declared `D_eff`; it is not a residual-energy fraction. The former direct
`q_ext -> Omega_b` and `95.14% -> dark abundance` chain remains tombstoned.

## Remaining closure programme

The next research-grade closure would need to preregister and derive: a
microscopic bath/drive and local energy current; a physical instrument and
record algebra; an error-controlled network-to-branching limit; a relativistic
local/covariant residual kernel with total-stress matching and no double
counting; and a frozen Einstein--Boltzmann likelihood for perturbations and
abundances. Until those are supplied, the strongest honest status is an
internally consistent conditional model with explicit falsifiers.

Formal result: `Gate: PASS` for the narrowed conditional scope after revision
1. It is not an arXiv-ready derivation of universal quantum ontology or dark
abundance.
