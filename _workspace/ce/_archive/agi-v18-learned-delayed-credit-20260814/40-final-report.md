# AGI V18 reward-decoded delayed credit: blocked contract report

Status: ABANDONED (E3 pathwise quantifier false after revision budget 2/2)

## Result

V18 did not reach implementation. The binary reward identity

$$
a(2R-1)=y
$$

and the coordinate recovery theorem $w=\theta$ survive. The homogeneous cross
block also exactly carries the cue. However, the strict control's preregistered
realized paired accuracy $1/2$ is false unless the entire post-checkpoint
nuisance trajectory and all random seeds are coupled across $q,-q$.

Equal marginal action laws imply expected accuracy $1/2$, not an exact
realized per-seed value. Because that premise was absent after two registered
contract revisions, the formal gate is `BLOCKED`. No code was implemented and
confirmation seeds 1,820,000--1,820,255 were never opened.

The successor must use a fresh seed block, whole-trajectory paired coupling,
state-matched rather than falsely compute-matched terminology, per-episode
reward timing checks, route-specific causal lesions, integer query labels and
a manifest that seals all imported behavior.

AGI, general delayed credit and infinite-SCC growth remain unestablished.
