# BA-TR10 final report

Status: `SYNTHETIC_LOCAL_STOCHASTIC_WEIGHT_CODE / DEVELOPMENT_GO /
ENDPOINT_CLOSED / CONFIRMATION_SEALED`.

The supplied fixed microscopic edge code of BA-TR9 was removed. Exact uniform
weights cannot break hidden permutation symmetry and correctly abstain. A
bounded, exchangeable fluctuation at the real delayed packet arrival plus a
local Oja update formed a durable source-column code in all 16 development
seeds. The code survived with jitter disabled, fast/homeostatic state reset,
and evaluation order reversed. Fourteen different permutations and all four
source-zero destinations were observed, excluding one fixed coordinate lookup.

The first unbounded-jitter equation failed calibration and was replaced before
development by a bounded factor admitting the analytic reuse-suppression
inequality. Homeostasis was materially useful: its ablation was bijective in
only 4/16 seeds.

This is not yet semantic memory or graph-morphology discovery. The `S` and `H`
groups and their candidate support are declared, output identity is absent, and
the learned permutation is stochastic. The next discriminating question is
whether this stored code produces a state-dependent geometric invariant, and
whether curvature carries information beyond the learned weight/metric itself.

