# Unified AGI breakthrough direction contract

Status: COMPLETE

## Objective

Enumerate the credible algorithmic breakthroughs left after V8, explain why
each differs from the exhausted output-shrinkage family, and combine the
smallest compatible subset into one development model architecture.

## Hard boundary

- Do not register V9.
- Do not allocate or simulate any new validation/test evidence block.
- Do not reopen V7 or V8 locked tests.
- Do not claim an AGI result from the four-chart forecasting family.
- Do not implement or benchmark the combined model in this run.

## Required output

1. A broad route map with expected mechanism, benefit, cost, and failure mode.
2. An explicit reject/defer/attach decision for every route.
3. One unified model with state, update, forecast, uncertainty, and stability equations.
4. A staged development order that identifies the first cheap falsification test,
   but does not execute it.

## Starting evidence

V8 showed that scalar, structured-gain, prefix-adaptive, and temporal
multi-origin readout corrections all return to the same local solution. The
next model must alter the represented predictive state or residual dynamics,
not merely retune `P + g(S-P)`.
