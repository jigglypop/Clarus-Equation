# BA-TR28 final

Status: `CONDITIONAL_LOW_DEGREE_PREDICTION_PASS /
RANDOM_PACKET_ENDPOINT_STOP / CONFIRMATION_CLOSED`.

The generic degree-two equation removed BA-TR27's finite $\mathbb Z_3$ table
and transferred every one of 400 rotating held-out contents at machine
precision.  The run nevertheless did not establish an operational routing
advantage over the affine baseline.  Randomly spaced packet candidates
quantized distinct continuous predictions to the same route too often; six
of 16 seeds exceeded the frozen 50-percent affine route ceiling.

This is an apparatus-resolution counterexample, not permission to relax the
ceiling.  BA-TR29 therefore retains the same learner and introduces fresh,
predeclared nearest-content and affine-derived hard-negative panels.  The
query-only $y_q+\delta$ world remains indistinguishable from the in-class
world, so neither run establishes unrestricted interaction discovery.
