# Clarus-Equation 에이전트 규약

이 파일은 모든 작업에 적용되는 짧은 하네스다. 이론·원고 작업은 `paper/README.md`,
연구 판정은 `paper/검증_원장/연구_목표_계약.md`를 먼저 따른다.

## 운영 규약

- 시작 시 적용 지침, 사용자 제약, 관련 파일과 Git 상태를 확인한다. 관련 없는 변경은
  보존하고, 요청 범위의 가장 작은 패치를 집중 검증까지 마친다.
- 사용자가 요구하지 않으면 스테이징·커밋·게시·이력 재작성·파괴적 정리를 하지 않는다.
- 루트 `gpt-6-astra`가 목표 해석, 가정 선택, 충돌, 연구 분기, 종합 판정을 소유한다.
  `gpt-5.6-sol` 작업자는 범위가 고정된 구현·조사·독립 검산만 맡고 물리 주장의 지위를
  바꾸지 않는다. 파일마다 작성자는 한 명만 둔다.
- 정의, 가정, 유도, 수치 산출, 경험적 적합, 반례, 열린 주장을 구분한다. 테스트 통과와
  수치 일치는 구현 증거이며 자연에 대한 물리 증명이 아니다.
- 위험에 비례한 최소 검사를 먼저 실행한다. 전체 회귀는 공용 경계 변경, 집중 검사 실패,
  또는 명시적 요청이 있을 때만 넓힌다.

## 연구 목표 — 달성 선언 아님

- `OBJ-01`: 하나의 상태공간·작용에서 양자론과 일반상대론을 연결하고 두 이론의 검증된
  극한을 모두 복원한다.
- `OBJ-02`: 비확정 양자 상태가 소멸하지 않고 비동기 틱의 비관측 성분으로 남다가 물리적
  관측 기록에서 확정되는 사상을 구성한다.
- `OBJ-03`: 비관측 상태의 존재공간·정규화·동역학·보존량·관측 투영을 정의하고 증명한다.
- `OBJ-04`: 전자기력·약력·강력을 공통장의 세 각도/투영으로, 중력을 전체 상태가 드러내는
  시공간 배경 반응으로 얻고 각 표준 극한을 복원한다.

## 검증 계약

- `VAL-01`: 같은 식·상태·공통 매개변수와 동일 자료·공분산·자유도 규칙으로 양자, 뮤온·
  입자, 암흑에너지·암흑물질·허블, 중력 블록의 공동 RMSE를 계산한다. 기준보다 전체가
  감소하고 사전 지정 필수 블록이 허용치 밖으로 악화하지 않는지 독립 계산으로 검산한다.
- `VAL-02`: 무피팅 예측을 우선한다. 필요한 보정은 훈련 자료에서만 수행해 자유도 비용과
  판본을 기록·동결하며, holdout·관측·후보별 재피팅으로 실패를 숨기지 않는다.

## 후보 가정 — 증명된 사실 아님

- `ASM-01`: 기초 구조가 무한차원·유한차원·무차원 표현인지, 확정/불확정 상태의 구분인지
  경쟁 판본으로 시험한다.
- `ASM-02`: 양자는 전체장 안의 ‘끼임’ 상태이며 비관측 동안 비동기 틱에 남고 기록으로
  현실에 나오면 확정된다는 후보를 시험한다.
- `ASM-03`: 시공간 경계에서 확정 가능한 상태의 제한이 최소 경계량 또는 경계 변분으로
  나타나 최소작용을 낳는다는 후보를 시험한다.

## 반례 분기

1. 계산 전에 후보의 정의·작용·가정 판본·예측·반증 조건을 기록한다.
2. 차원, 보존, 완전양성, 국소성, 표준 극한과 공동 RMSE를 계산하고 독립 검산한다. 악화되었을 시 다른 공리를 세워서 돌파한다.
3. 명확한 반례나 더 단순한 대안이 나오면 피팅으로 봉합하지 않는다. 해당 판본을 기각
   또는 적용범위 축소로 보존하고 다음 가정을 별도 분기로 연다.
4. 생존 후보만 다음 강도 검증으로 올린다. 새로 증명된 좁은 주장은 번호 장 또는
   `paper/검증_원장/`에 전제·유도·검산·오차와 함께 기록한다.
