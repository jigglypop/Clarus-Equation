---
description: 수치·회귀 검증 실행 (validate)
---

validate 스킬로 검증을 실행하라.

범위: $ARGUMENTS (비어 있으면 수정 영역 기준으로 최소 범위를 고른다)

작은 테스트부터 시작해 관련 회귀로 넓히고, 이전 기준과 달라진 수치를 최상단에 보고한다. 기계 결과를 이론 지위로 해석하지 않는다. 전체 pytest는 사용자가 `전체`/`full`을 명시할 때만.
