# CE docs 전체 완성도 감사

## 1. 감사 범위

대상은 `docs/` 아래 Markdown 문서 전체다.

| 항목 | 값 |
|---|---:|
| Markdown 파일 수 | 212 (2026-08-06 정합성 완성 루프 기준) |
| 총 규모 | 약 3.85 MB, 약 8.9만 줄 |
| 최상위 정본 | `README.md`, `axium.md`, `경로적분.md`, `상수.md` |
| 가장 큰 폴더 | `7_AGI/` 약 486 KB |
| 문서형 실험 게이트가 가장 강한 폴더 | `6_뇌/`, `3_상수/` |
| 응용 확장이 가장 넓은 폴더 | `4_공학적_활용/`, `7_AGI/` |

기계 점검 결과:

| 항목 | 2026-08-06 교정 전 발견 | 완료 gate |
|---|---:|---|
| H1 없는 문서 | 18 | 0 |
| H1이 여러 개인 문서 | 1 이상 | code fence를 제외하고 0 |
| 실제 누락 로컬 target | 1개, 2회 참조 | 0; Q0 manifest 복원 |
| unmatched display-math/code fence | 0 | 0 유지 |
| URL/arXiv/DOI 토큰이 없는 파일 | 165 | 경험적 주장 파일에 provenance 추가 |
| 수식·차원 직접 반례 | core/particle/cosmology/application에 다수 | 완성 루프 G2/G3 통과 |

## 2. 전체 판정

주관적 백분율 점수는 직접 반례와 문서 형식 문제를 같은 척도로 더하므로
폐기한다. 현재 완성도는
[전체 정합성 완성 루프](FULL_CONSISTENCY_COMPLETION_LOOP_2026-08-06.md)의
G1--G6 gate로 판정한다.

가장 큰 장점은 다음이다.

- `README.md`가 오일러 항등식의 지위를 "직접 증명식"이 아니라 "최소 생성 문법"으로 낮춰 과잉 주장을 잘 막는다.
- `상수.md`가 `Exact / Selection / Bridge / Phenomenology / Open / Open test` 등급을 도입해 수치 결과의 주장 강도를 분리한다.
- `경로적분.md`는 계산 체인의 중심 문서로 충분한 밀도와 교차 검증 항목을 가진다.
- `6_뇌/`와 `7_AGI/`는 구조 유비를 실험 게이트와 런타임 사양으로 낮춰 읽으려는 장치가 있다.
- `4_공학적_활용/09_무차원_잔차장_환각억제.md`와 `08_기상_그래프잔차_연구.md`는 응용 문서 중 논문형으로 승격하기 쉬운 편이다.

가장 큰 약점은 다음이다.

- 많은 하위 문서가 초록, 기여, 반증 조건, 한계, 재현성 블록 없이 바로 수식으로 들어간다.
- 일부 문서는 `Exact`, `Bridge`, `Phenomenology`가 문장 안에서 다시 섞이며, 독자가 어느 세기의 주장인지 즉시 알기 어렵다.
- `README.md`의 폴더 구조 표가 `7_AGI/`, `8_리만/`을 별도로 설명하지 않아 현재 실제 규모와 맞지 않는다.
- 코드 경로가 레거시와 현재 checkout 사이에서 흔들린다. 실제 파일이 없는 `examples/...`, `quant/...` 참조는 "현재 구현"이 아니라 "과거 패턴 또는 추가 예정"으로 표시해야 한다.
- 외부 논문 형식의 참고문헌, 데이터 provenance, 재현 스크립트, 결과 JSON 규약이 폴더 전체에 균일하지 않다.

## 3. 폴더별 완성도

| 폴더 | gate 관점 판정 | 보강 핵심 |
|---|---|---|
| root docs | canonical contract synchronized | Track A/B, 수치 manifest, 타입·입력 provenance와 판정 규약 고정 |
| `1_강의/` | synchronized teaching layer | 교정본보다 강한 문장 회귀를 G2/G3가 차단 |
| `2_경로적분과_응용/` | replacement models documented | strong CP·inflation·flavour의 현행 작용·입력·검증 조건 명시 |
| `3_상수/` | canonical equations synchronized | 결합상수 두 트랙과 공동 관측 baseline 고정 |
| `4_공학적_활용/` | dimensional contracts synchronized | Casimir·cycle energy·band EFT와 핵융합 fail-closed gate 명시 |
| `5_유도/` | theorem/EFT contracts synchronized | H1, 작용 provenance, 경계조건과 열린 Wilson 계수 명시 |
| `6_뇌/` | empirical protocols synchronized | 실제 데이터 provenance와 holdout은 연구 실행 과제 |
| `7_AGI/` | algebra/runtime contracts synchronized | simplex·canonical spec·실행 gate 명시 |
| `8_리만/` | mathematical scope synchronized | phase convention·Hermiticity와 RH 비증명 범위 고정 |
| `9_등호이전/` | conditional mathematics synchronized | 현행 수치 override와 legacy runtime 분리 |
| `참조/` | historical/support isolated | superseded 수치는 역사 snapshot으로만 허용 |

## 4. 우선순위

### P0: 정본 지도 갱신 — 완료

`README.md`와 감사 인덱스가 현행 정본, canonical manifest, 전체 gate를
연결한다. 레거시 실행 경로는 현행 증거로 승인하지 않는 규칙도 명시했다.

### P1: 선택적 status box 확장

현재 전수 gate는 제목·링크·수식·표·semantic sentinel을 검사한다. 아래
status box는 독립 배포할 문서의 가독성을 높이는 선택적 metadata이며,
정합성 완료 여부를 뒤집는 미완료 gate가 아니다.

```text
상태: Exact / Selection / Bridge / Phenomenology / Open / Spec
역할: 정본 / 강의 / 응용 / 실험 프로토콜 / 참고
입력: 이 문서가 전제하는 값과 문서
출력: 이 문서가 새로 주장하는 값과 식
검증: 관측, 코드, 실험, 반증 조건
```

우선 적용 대상:

1. `2_경로적분과_응용/*.md`
2. `4_공학적_활용/*.md`
3. `5_유도/*.md`
4. `7_AGI/*.md`

### P2: H1 구조 — 완료

모든 Markdown 파일은 code fence 밖에서 정확히 하나의 H1을 갖는다.
구조 gate가 누락과 복수 H1의 회귀를 차단한다.

### P3: 반증 조건 표준화 — 핵심 정본 완료

핵심 정본과 감사 루프는 최소한 다음 fail-closed 규칙을 공유한다.

| 반증 조건 | 해당 주장 | 판정 기준 | 다음 조치 |
|---|---|---|---|
| 사전등록 관측 gate 실패 | Bridge 식 | `REJECT` | 입력·작용·대체 브리지를 새 revision에서 재검증 |
| shuffled/flat baseline보다 낮지 않음 | 응용 gate | `REJECT` 또는 진단 전용 | graph 항 제거 또는 대조군으로 제한 |
| holdout에서 무너짐 | 일반화 주장 | reject | train-only 보정으로 재분류 |

### P4: 코드와 문서 경로 일치 — 외부 workspace 후속

현재 docs 밖 runtime의 일부 상수·테스트는 legacy fixture를 유지한다. 문서는
이를 canonical 계산으로 부르지 않고 다음 세 등급으로 분리한다.

| 표기 | 의미 |
|---|---|
| 현재 구현 | 현재 checkout에 실제 파일 존재 |
| 과거 패턴 | 과거 구현 또는 문맥 추출에는 있으나 현재 파일 없음 |
| 추가 예정 | 설계상 필요하지만 아직 없음 |

특히 구 density runner는 원고 canonical manifest와 분리되며, 코드 쓰기
권한이 생기면 상수·expected test snapshot을 함께 재생성해야 한다.

### P5: 외부 제출용 논문 후보 분리

현재 폴더 전체를 하나의 논문으로 내기보다는, 제출 후보를 분리하는 편이 낫다.

| 후보 | 현재 준비도 | 이유 |
|---|---:|---|
| CE 상수/우주론 scorecard | 문서·산술 완료, 물리 적합 실패 | 결합상수 순환은 제거했으나 fixed-background DESI gate가 `REJECT` |
| H0 source-role readout | 중간-높음 | audit/paper draft 구조가 있음 |
| 무차원 hallucination gate | 중간 | 수식 명료, 구현/벤치만 붙이면 됨 |
| 기상 graph residual gate | 중간 | 예비 수치와 프로토콜 있음, 재현 패키지 필요 |
| 뇌-AGI runtime spec | 중간 | 문서량은 많지만 주장 층위 분리가 더 필요 |
| Riemann/MRA | 재구성 중 | canonical phase와 Hermiticity 조건 통일 필요 |
| 핵융합/초전도/진공에너지 공학 | 낮음-중간 | 아이디어는 많지만 실험 현실성, 비용, 반증 조건 보강 필요 |

## 5. 실행 상태

1. 완료: 전체 정합성 완성 루프와 실행 gate를 정본 인덱스에 연결.
2. 완료: 누락 Q0.0--Q0.3 manifest와 단일 numeric manifest 복원.
3. 완료: H1·링크·수식 delimiter·GFM 표·제어문자 전수 교정.
4. 완료: A3b·bootstrap·PDE 직접 반례를 정확한 정리·작용으로 교체.
5. 완료: 결합상수·strong CP·flavour·우주론의 입력·조건부 작용·provenance 재구성.
6. 완료: Casimir·초전도·AGI simplex·Riemann/MRA 및 핵융합 수식 계약 교체.
7. 완료: 정적 문서 gate, canonical 산술 verifier와 핵심 회귀 실행.
8. 후속 연구: `REJECT/Open`인 물리 bridge의 새 작용·독립 자료·holdout을
   확보하고, docs 밖 legacy runtime snapshot을 canonical 계약으로 이관.

## 6. 결론

`docs/`는 이미 단순 아이디어 모음은 아니다. 핵심 정본, 수치 집계, 응용 문서, 뇌/AGI 사양까지 갖춘 큰 연구 노트다. 다만 완성도를 가르는 병목은 "내용 부족"이 아니라 **주장 강도와 재현성의 균일성 부족**이다.

전체 문서군은 다음 세 층을 유지하되 한 번의 완성 gate로 연결한다.

1. `경로적분.md` + `상수.md`: CE core scorecard.
2. `2_경로적분과_응용/`, `3_상수/`: core의 하위 유도와 관측량 세부 근거.
3. `4_공학적_활용/`, `5_유도/`, `6_뇌/`, `7_AGI/`, `8_리만/`: Application Bridge와 실험 후보.

이렇게 나누면 각 주장을 지지하는 정확한 수식·입력·실험 gate를 추적할 수
있고, 상태 라벨 변경이 아니라 실제 대체 모형의 통과로 완성도를 올릴 수 있다.
