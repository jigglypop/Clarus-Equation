# 우주론 수식의 의미와 후보 원리

## 1. 분석 결과

현재 우주론 게이트 A-E의 핵심 수식들은 서로 모순되지 않는다. 고쳐야 할 대수 오류는 보이지 않는다. 다만 수식의 의미가 한 종류가 아니라, 관측 채널에 따라 서로 다른 읽기 층으로 나뉜다는 점이 중요하다.

따라서 이번 장의 결론은 다음이다.

> CE 우주론은 하나의 수식을 모든 관측량에 같은 방식으로 대입하는 이론이 아니라, 자기재귀 고정점에서 나온 상태량, 총 민감도, 잔차 구동력, 동결 잔차, 후기 추출 잔차를 구분해 읽어야 하는 이론이다.

이 구분을 하지 않으면 \(A_s\)처럼 raw 식이 크게 실패한 항목을 잘못 닫힌 예측으로 읽게 된다. 반대로 이 구분을 명확히 하면 어떤 항목이 닫혔고 어떤 항목이 후보인지 정직하게 분리된다.

---

## 2. 자기재귀 고정점의 의미

기본 식은

$$
x=e^{-(1-x)D}
$$

이다. 여기서

$$
x=\varepsilon^2,\qquad \sigma=1-x
$$

로 둔다.

이 식의 의미는 다음 순환이다.

$$
x
\longrightarrow
\sigma=1-x
\longrightarrow
e^{-\sigma D}
\longrightarrow
x.
$$

즉 생존율 \(x\)가 억압률 \(\sigma\)를 만들고, 억압률이 다시 생존율을 결정한다. 그래서 이 식은 단순 감쇠식이 아니라 자기재귀 고정점 조건이다.

이 자기재귀성은 철학적 은유가 아니라 풀 경로 보존 조건에서 나오는 최소 닫힘이다. 경로공간을 \(\Gamma\), 그 위의 측도를 \(\mu\)라 하면, 일반 경로적분은

$$
Z=\int_\Gamma\mathcal D\gamma\,e^{-S[\gamma]}
$$

처럼 \(\Gamma\) 위의 정보를 하나의 수로 압축한다. 반대로 CE가 풀 경로를 계속 살리려면 접힘 연산자가

$$
\mathcal T_D:\mathcal M(\Gamma)\to\mathcal M(\Gamma)
$$

처럼 경로 측도공간 안에서 닫혀야 한다. 외부 선택자 없이 총 생존량만 읽으면

$$
x[\mu]=\frac{\int_\Gamma d(\mathcal T_D\mu)}{\int_\Gamma d\mu}
$$

이고, 억압 총량은 내부 여집합으로

$$
\sigma[\mu]=1-x[\mu]
$$

가 된다. \(D\)개의 독립 접힘 깊이가 같은 억압률을 곱적으로 누적하면

$$
x=\exp(-D\sigma)
$$

이므로

$$
\boxed{x=\exp[-D(1-x)]}
$$

가 된다.

즉 현재 우주론 식의 가장 선명한 수학적 근거는 다음이다.

$$
\boxed{
\text{풀 경로 보존}
+\text{외부 선택자 금지}
+\text{차원 깊이의 곱적 감쇠}
\Rightarrow
\text{자기재귀 고정점}
}
$$

다만 이것은 최소 kernel \(K[x]=1-x\)를 택한 결과다. 더 넓은 이론에서는 \(x=\exp[-DK[x]]\) 형태의 비국소 또는 scale-dependent kernel이 가능하므로, \(K[x]=1-x\)의 유일성은 아직 `Open`이다.

현재 우주론 값은

$$
D_{\rm eff}=3.17775842,
$$

$$
x=0.04864672,
\qquad
\sigma=0.95135328.
$$

따라서 가장 안전한 의미 부여는 다음이다.

| 기호 | 의미 | 현재 지위 |
|---|---|---|
| \(x\) | \(d=3\) 표준 물질 채널로 살아남은 생존 고정점 | `Selection/Bridge` |
| \(\sigma=1-x\) | \(d=3\)으로 결정화되지 못한 암흑 부문 총량 | `Selection/Bridge` |
| \(D_{\rm eff}\) | 정수 차원 \(3\)과 결핍지수 \(\delta\)가 결합한 유효 재귀 깊이 | `Bridge` |

이 해석에서

$$
\Omega_b=x=0.04864672
$$

는 넣은 값이 아니라 생존 고정점이다.

물리적으로 보면, \(\Omega_b=x\)는 "어떤 경로 하나가 선택되었다"는 말이 아니다. 전체 경로 앙상블이 자기 억압률을 만들고, 그 억압률을 통과한 뒤에도 표준 물질 채널로 읽히는 총 생존 측도가 \(x\)라는 뜻이다. 따라서 CE에서 관측되는 우주는 한 경로의 승자가 아니라, 풀 경로공간이 자기 자신을 닫은 뒤 남긴 안정한 읽기값이다.

이 점에서 CE는 세 접근과 구분된다.

| 접근 | 경로 처리 | 정보 손실 |
|---|---|---|
| stationary phase | 작용이 정지하는 경로 근방만 남김 | 큰 손실 |
| Wilsonian coarse graining | 고에너지/짧은 스케일 자유도를 적분해 유효 작용으로 넘김 | 통제된 손실 |
| CE 자기재귀 닫힘 | 전체 경로 측도공간 안에서 생존량과 억압량을 함께 닫음 | 스칼라 읽기에서는 손실, 경로공간 닫힘은 유지 |

그래서 CE의 핵심 물리 명제는 다음처럼 쓸 수 있다.

$$
\boxed{
\text{관측량은 전체 경로의 폐기가 아니라, 전체 경로공간의 자기일관 잔차를 읽는다.}
}
$$

---

## 3. \(d=0\)의 의미

\(D=0\)에서는

$$
x_0=1,\qquad \sigma_0=0.
$$

재귀 엔트로피를

$$
\mathcal S_{\mathcal R}=-\log x
$$

로 두면

$$
\mathcal S_{\mathcal R,0}=0.
$$

물리 가지에서는

$$
\mathcal S_{\mathcal R}
=-\log(0.04864672)
=3.02317090.
$$

즉 \(d=0\)은 "아무것도 없음"보다 더 정확히는

$$
\boxed{
d=0=\text{zero-recursive-entropy identity boundary}
}
$$

이다. 반대로 \(d=3\) 물리 우주는

$$
\boxed{
d=3=\text{finite-recursive-entropy contracted branch}
}
$$

로 읽는다.

이 해석은 초기 특이점 문제에 대한 후보 방향을 준다. 하지만 아직 일반상대론의 특이점 정리를 대체하지는 않는다. 절대 열역학 엔트로피, FLRW 곡률 초기값, horizon entropy로 옮기는 스케일 매핑이 필요하다.

0차원 항등성의 성질은 세 가지로 압축된다.

| 성질 | 수식 | 3차원에 남는 영향 |
|---|---|---|
| 완전 생존성 | \(x_0=1\) | 표준 물질 생존률 \(x_D<1\)의 기준 |
| 무결핍성 | \(\sigma_0=0\) | 암흑 부문 총량 \(\sigma_D=1-x_D\)의 기준 |
| 무재귀엔트로피 | \(\mathcal S_{\mathcal R,0}=0\) | 재귀 엔트로피 \(\mathcal S_{\mathcal R}=-\log x_D\)의 기준 |

따라서 3차원이 0차원을 지향한다는 말은 \(d=3\) 공간이 실제로 \(d=0\) 위치로 움직인다는 뜻이 아니다. 물리적으로 가능한 것은 3차원 수축 가지 안에서 국소 잔차를 줄이는 것이다.

현재 값에서 수축률은

$$
k_D=D_{\rm eff}x_D=0.15458752.
$$

따라서 고정점 근방 잔차는

$$
\eta_{n+1}\simeq k_D\eta_n
$$

로 줄어든다. 하지만 \(d=0\) 항등상 \(x_0=1\)은 같은 수축 basin 안의 점이 아니라 Lambert \(W_{-1}\) 경계 가지다. 그러므로 유한한 3차원 물리 동역학으로 \(d=0\) 자체에 도달한다고 말할 수는 없다.

현재 판정은 다음이다.

$$
\boxed{
d=0\text{은 목적지가 아니라 }d=3\text{ 잔차 흐름의 거울 경계조건이다.}
}
$$

### \(d=0\) 존재성 검증 게이트

\(d=0\)을 확실히 아는 방법은 직접 관측이 아니다. \(d=0\)은 \(d=3\) 안의 사건이나 위치가 아니라 경계 가지이므로, 직접 관측 대상이 되기 어렵다. 검증 가능한 질문은 더 약하다.

$$
\boxed{
d=0\text{을 zero-residual boundary로 두었을 때 }d=3\text{의 필수 흔적들이 동시에 맞는가?}
}
$$

이를 `examples/physics/d0_boundary_verification_gate.py`로 검사했다.

검증 분류는 다음과 같다.

| 질문 | 판정 | 이유 |
|---|---|---|
| 수학적 경계해가 있는가 | yes | \(D=0\Rightarrow x_0=1,\sigma_0=0,\mathcal S_{\mathcal R,0}=0\) |
| \(d=0\)이 장소/상태로 직접 관측되는가 | no | \(d=3\) 사건이 아니라 경계 가지 |
| \(d=0\)을 물리적으로 추론할 수 있는가 | candidate | \(d=3\) 흔적 묶음으로만 가능 |
| 보통의 \(d=3\) 동역학으로 도달 가능한가 | no | 물리 가지는 \(W_0\), \(d=0\)은 \(W_{-1}\) 경계 가지 |

계산 결과는 다음이다.

| 흔적 | 예측 | 기준 | pull |
|---|---:|---:|---:|
| \(\Omega_b=x\) | \(0.04864672\) | \(0.0493\pm0.0004\) | \(-1.63\sigma\) |
| \(n_s=1-2/N_e\) | \(0.96503475\) | \(0.9649\pm0.0042\) | \(+0.03\sigma\) |
| \(A_s\) raw total sensitivity | \(7.8353\times10^{-9}\) | \(2.10\times10^{-9}\) | fail |
| \(A_s\) projected residual | \(2.1038\times10^{-9}\) | \(2.10\times10^{-9}\) | \(+0.13\sigma\) |

또한 재귀 정체성 자체는

$$
\mathcal S_{\mathcal R}-D_{\rm eff}(1-x)=-2.22\times10^{-15}
$$

수준으로 닫히며, 역방향 한 단계는

$$
1/k_D=6.46882737
$$

만큼 증폭된다. 따라서 \(d=0\)로 되돌아가는 과정은 안정 수축 동역학이 아니라 비수축 역접힘이다.

이 게이트의 결론은 조심스럽다.

$$
\boxed{
d=0\text{의 직접 존재는 증명되지 않았다.}
}
$$

하지만

$$
\boxed{
d=0\text{은 }d=3\text{ 물리의 zero-residual boundary condition으로는 살아남는다.}
}
$$

반증 조건도 명확하다. \(n_s\), running, tensor, 또는 \(A_s\) 잔차 읽기 원리가 깨지면 \(d=0\)은 물리 경계조건 후보에서 내려와 형식적 이상점으로 남는다.

### 강건성 및 반례 게이트

다음으로 `examples/physics/d0_boundary_robustness_gate.py`에서 더 공격적인 반례를 세웠다. 질문은 두 가지다.

1. \(\delta=0\)인 순수 \(d=3\) null도 같은 정도로 맞는가?
2. 최소 kernel \(K[x]=1-x\)가 강제되는가, 아니면 \(K[x]=c(1-x)\) 조정으로 대체 가능한가?

결과는 다음이다.

| 모델 | \(D\) | \(c\) | \(x\) | \(\Omega_b\) pull | \(n_s\) pull | \(\chi^2(\Omega_b,n_s)\) |
|---|---:|---:|---:|---:|---:|---:|
| plain \(d=3\), no \(\delta\) | \(3.00000000\) | \(1.00000000\) | \(0.05952021\) | \(+25.55\sigma\) | \(-0.46\sigma\) | \(653.042\) |
| CE minimal kernel | \(3.17775842\) | \(1.00000000\) | \(0.04864672\) | \(-1.63\sigma\) | \(+0.03\sigma\) | \(2.668\) |
| kernel tuned to \(\Omega_b\) | \(3.17775842\) | \(0.99627164\) | \(0.04930000\) | \(0.00\sigma\) | \(+0.03\sigma\) | \(0.001\) |

이 결과는 양면적이다.

- \(\delta=0\) null은 \(\Omega_b\)에서 강하게 깨진다. 따라서 \(d=3\) 물리 가지에는 결핍 흔적 \(\delta\)가 필요하다.
- 최소 kernel \(c=1\)은 자연스럽고 살아남지만, 작은 \(c\) 조정으로 \(\Omega_b\)만 맞추는 것은 가능하다. 따라서 \(K[x]=1-x\)의 유일성은 아직 증명되지 않았다.
- \(A_s\) total sensitivity는 여전히 \(+191\sigma\)로 기각되고, projected residual은 \(+0.13\sigma\)로 살아남는다. 즉 \(d=0\) 경계 해석은 readout taxonomy 없이는 닫히지 않는다.

따라서 현재의 가장 정직한 지위는 다음이다.

$$
\boxed{
d=0\text{은 직접 실재라기보다, }\delta\text{ 결핍과 잔차 읽기 구조를 요구하는 강한 경계조건 후보이다.}
}
$$

$$
\boxed{
\text{다음 hard test는 running/tensor와 }\mathcal S_{\mathcal R}\to\text{FLRW scale map이다.}
}
$$

### FLRW scale map 게이트

`examples/physics/flrw_scale_map_gate.py`에서 \(\mathcal S_{\mathcal R}\)를 FLRW 물리량으로 옮기는 최소 후보들을 검사했다. 핵심 결과는 "일부 무차원 비율은 닫히지만, 절대 엔트로피는 닫히지 않는다"이다.

CE 코어 값은

$$
\mathcal S_{\mathcal R}=3.02317090,\qquad
k=D_{\rm eff}x=0.15458752,\qquad
N_e=57.19965162.
$$

곡률/평탄성 쪽에서는 두 무차원 억압비가 바로 계산된다.

| map | 식 | 값 | 판정 |
|---|---|---:|---|
| 표준 e-fold 곡률 희석 | \(e^{-2N_e}\) | \(2.07498\times10^{-50}\) | 무차원으로 닫힘 |
| 재귀 잔차 희석 | \(k^{N_e}\) | \(4.17888\times10^{-47}\) | 무차원으로 닫힘 |
| \(k^n<10^{-5}\) 반복수 | \(\log(10^{-5})/\log k\) | \(6.17\) | 평탄성은 쉽게 억압 |
| \(k^n<10^{-60}\) 반복수 | \(\log(10^{-60})/\log k\) | \(74.00\) | 깊은 잔차 지우기 |

즉 \(d=0\) 경계조건은 FLRW의 "평탄성 방향"과는 잘 맞는다. 하지만 이것은 \(\Omega_k\)의 정확한 오늘 값 예측이 아니라, 초기 곡률 잔차가 강하게 희석된다는 무차원 구조다.

인플레이션 스케일은 \(A_s\), \(r\), \(M_{\rm Pl}\)을 공급하면 lift된다.

| 양 | 값 | 판정 |
|---|---:|---|
| \(A_s^{\rm A3c}\) | \(2.10381\times10^{-9}\) | 후보 |
| \(H_{\rm inf}/M_{\rm Pl}\) | \(6.16512\times10^{-6}\) | \(A_s,r\) 필요 |
| \(H_{\rm inf}\) | \(1.50121\times10^{13}\,{\rm GeV}\) | scale-lifted |
| \(H_{\rm inf}\) using A3c | \(1.50257\times10^{13}\,{\rm GeV}\) | 후보 |
| \(V^{1/4}\) | \(7.95701\times10^{15}\,{\rm GeV}\) | scale-lifted |

반대로 horizon entropy는 \(\mathcal S_{\mathcal R}\)만으로는 닫히지 않는다. \(H_0\)와 \(t_{\rm Pl}\)를 외부 스케일로 넣으면

$$
S_{\rm dS}\simeq \frac{\pi}{(H_0t_{\rm Pl})^2}
=2.26544\times10^{122},
$$

$$
\log S_{\rm dS}=281.73315068.
$$

이는

$$
\frac{\log S_{\rm dS}}{\mathcal S_{\mathcal R}}=93.19127498,
\qquad
\frac{\log S_{\rm dS}}{N_e}=4.92543473
$$

의 추가 lift factor를 요구한다. 따라서 \(\mathcal S_{\mathcal R}\)는 물리 horizon entropy 그 자체가 아니다.

현재 판정은 다음이다.

$$
\boxed{
\mathcal S_{\mathcal R}\text{는 FLRW 평탄화 비율과 안정성에는 연결되지만, 절대 horizon/reheating entropy에는 아직 연결되지 않는다.}
}
$$

즉 \(d=0\)은 여전히 무차원 경계조건이지, 완전한 FLRW 초기상태 정리는 아니다.

### horizon entropy lift 후보

위 열린 문제를 더 압박하기 위해 `examples/physics/horizon_entropy_lift_gate.py`를 추가했다. 관측 \(H_0=67.4\,{\rm km\,s^{-1}Mpc^{-1}}\)와 \(t_{\rm Pl}\)를 넣으면

$$
\log S_{\rm dS}=281.73315068,
\qquad
\frac{\log S_{\rm dS}}{N_e}=4.92543473.
$$

그런데

$$
\frac{\pi^2}{2}=4.93480220
$$

와 매우 가깝다. 순수 leading 후보

$$
\log S_{\rm dS}^{(0)}=\frac{\pi^2}{2}N_e
$$

는

$$
\log S_{\rm dS}^{(0)}=282.26896669
$$

로 실제보다 \(+0.53581601\) 크고, 이에 대응하는 \(H_0\)는 \(51.56\,{\rm km\,s^{-1}Mpc^{-1}}\)라서 너무 낮다.

하지만 결핍-암흑 보정

$$
\boxed{
\log S_{\rm dS}^{\rm cand}
=
\frac{\pi^2}{2}N_e-\pi\delta\sigma
}
$$

를 넣으면

$$
\log S_{\rm dS}^{\rm cand}=281.73768863
$$

이고,

$$
H_0^{\rm cand}=67.247245\,{\rm km\,s^{-1}Mpc^{-1}}.
$$

이는 기준값 \(67.4\) 대비 \(-0.227\%\)이다.

비교 후보들은 다음과 같다.

| lift 후보 | \(H_0\) 오차 |
|---|---:|
| \((\pi^2/2)N_e\) | \(-23.50\%\) |
| \((\pi^2/2)N_e-\pi\delta\) | \(+1.14\%\) |
| \((\pi^2/2)N_e-\pi\delta\sigma\) | \(-0.23\%\) |
| \((\pi^2/2)N_e-\pi\delta(1-x/2)\) | \(+0.45\%\) |
| \((\pi^2/2)N_e-\mathcal S_{\mathcal R}/(2\pi)\) | \(-2.70\%\) |

현재 판정은 강한 후보지만 아직 정리는 아니다.

$$
\boxed{
\log S_{\rm dS}\simeq(\pi^2/2)N_e-\pi\delta\sigma
}
$$

가 진짜라면 \(d=0\) 경계조건은 단순한 무차원 평탄화 조건을 넘어 horizon entropy lift까지 건드린다. 반대로 \(\pi^2/2\) phase-area lift와 \(\pi\delta\sigma\) boundary correction을 독립적으로 유도하지 못하면, 이 결과는 수치 패턴으로만 남긴다.

#### 조건부 유도

`examples/physics/horizon_entropy_lift_derivation_gate.py`에서는 이 후보를 두 반주기 적분으로 다시 썼다.

첫째, e-fold 하나가 horizon log-entropy에 기여하는 기본 위상면적을

$$
A_{\rm phase}
=
\int_0^\pi \theta\,d\theta
=
\frac{\pi^2}{2}
$$

로 둔다. 둘째, 같은 반주기 위의 결핍 경계밀도를 \(\delta\sigma\)로 두면

$$
B_{\rm defect}
=
\int_0^\pi \delta\sigma\,d\theta
=
\pi\delta\sigma.
$$

따라서 조건부로

$$
\boxed{
\log S_{\rm dS}
\simeq
N_e A_{\rm phase}-B_{\rm defect}
=
\frac{\pi^2}{2}N_e-\pi\delta\sigma
}
$$

가 된다.

수치 검산은

$$
B_{\rm defect}=0.53127806,
\qquad
\log S_{\rm pred}=281.73768863,
$$

$$
\Delta\log S=+0.00453795,
\qquad
H_0^{\rm pred}=67.247245\,{\rm km\,s^{-1}Mpc^{-1}}.
$$

역으로 필요한 경계밀도를 계산하면

$$
\rho_{\rm req}
=
\frac{(\pi^2/2)N_e-\log S_{\rm dS}}{\pi}
=0.17055553,
$$

후보 밀도는

$$
\rho_{\rm cand}=\delta\sigma=0.16911106
$$

로 \(-0.847\%\) 차이다.

따라서 이 후보의 지위는 `Raw numerology`에서 `Conditional/Bridge`로 올라간다. 그러나 아직 `Exact`는 아니다. 남은 핵심 가정은 다음이다.

$$
\boxed{
\text{왜 horizon log-entropy가 e-fold당 반주기 위상면적 } \int_0^\pi\theta d\theta\text{를 세는가?}
}
$$

#### phase-area 계수의 내부 고정

`examples/physics/phase_area_principle_gate.py`에서는 \(\pi^2/2\) 자체가 CE 문법 안에서 임의 계수가 아닌지 확인했다. 두 구성이 정확히 일치한다.

첫째, 반주기 순서 위상면적은

$$
\int_0^\pi\theta\,d\theta=\frac{\pi^2}{2}.
$$

둘째, \(d\)차원 adjoint-normalized full phase square는

$$
\frac{(2\pi)^2}{d^2-1}.
$$

두 값이 같으려면

$$
\frac{(2\pi)^2}{d^2-1}=\frac{\pi^2}{2}
$$

이므로

$$
d^2-1=8,\qquad d=3.
$$

수치 비교는 다음이다.

| \(d\) | \((2\pi)^2/(d^2-1)\) | \(\pi^2/2\) 대비 |
|---:|---:|---:|
| 2 | \(13.15947253\) | \(+166.667\%\) |
| 3 | \(4.93480220\) | \(0.000\%\) |
| 4 | \(2.63189451\) | \(-46.667\%\) |
| 5 | \(1.64493407\) | \(-66.667\%\) |

따라서 horizon lift의 leading 계수는

$$
\boxed{
\frac{\pi^2}{2}
=
\int_0^\pi\theta d\theta
=
\frac{(2\pi)^2}{3^2-1}
}
$$

로 \(d=3\)에서 내부 고정된다. 이 결과는 phase-area lift를 지지한다. 다만 "horizon log-entropy가 이 계수를 e-fold마다 읽는다"는 readout 원리는 여전히 `Bridge`다.

#### readout 원리 압박

`examples/physics/horizon_readout_principle_gate.py`에서는 여러 가능한 readout을 같은 경계 보정 \(\pi\delta\sigma\) 아래에서 비교했다.

| readout | e-fold당 계수 | \(H_0\) 결과 | 판정 |
|---|---:|---:|---|
| linear e-fold count | \(1\) | \(5.02\times10^{50}\) | 실패 |
| curvature dilution count | \(2\) | \(1.91\times10^{38}\) | 실패 |
| mean half-cycle phase | \(\pi/2\) | \(4.08\times10^{43}\) | 실패 |
| half-cycle survival projection | \(2/\pi\) | \(1.64\times10^{55}\) | 실패 |
| half-cycle ordered area | \(\pi^2/2\) | \(67.247245\) | 생존 |
| full-cycle ordered area | \(2\pi^2\) | \(8.83\times10^{-183}\) | 실패 |
| \(d=3\) adjoint phase measure | \((2\pi)^2/8\) | \(67.247245\) | 생존 |

역으로 필요한 계수는

$$
c_{\rm req}
=
\frac{\log S_{\rm dS}+\pi\delta\sigma}{N_e}
=4.93472287.
$$

이는

$$
\frac{\pi^2}{2}=4.93480220
$$

와 \(+0.002\%\) 차이다.

또한 이 readout은 표준 slow-roll의 국소 horizon entropy 증가와 다르다. 일반적인 quasi-de Sitter 진화에서는

$$
S_{\rm hor}\propto H^{-2}
\quad\Rightarrow\quad
\frac{d\log S_{\rm hor}}{dN}=2\epsilon_H.
$$

CE의 \(\pi^2/2\)는 slow-roll \(\epsilon_H\) 항으로 읽기에는 너무 크다. 따라서 이 식은 인플레이션 중 국소 horizon entropy 성장식이 아니라,

$$
\boxed{
\text{primordial e-fold phase count}\to\text{late-time de Sitter entropy}
}
$$

라는 boundary lift로 읽어야 한다.

현재 지위는 더 좁아진다.

$$
\boxed{
\text{계수 선택은 거의 고정되었다. 남은 가정은 late-time horizon이 왜 primordial phase-area count를 읽는가이다.}
}
$$

#### early-late boundary invariant

`examples/physics/early_late_boundary_invariant_gate.py`에서는 남은 가정을 보존량 형태로 바꿔 검사했다. 제안하는 경계 불변량은

$$
I_H=\log S_{\rm dS}+\pi\delta\sigma
$$

이고, 원시 위상 카운트는

$$
I_{\rm phase}=\frac{\pi^2}{2}N_e.
$$

경계 readout 원리는 다음 등식으로 정리된다.

$$
\boxed{
\log S_{\rm dS}+\pi\delta\sigma
\simeq
\frac{\pi^2}{2}N_e
}
$$

앞방향으로 쓰면

$$
\log S_{\rm pred}=281.73768863,
\qquad
H_0^{\rm pred}=67.247245\,{\rm km\,s^{-1}Mpc^{-1}}.
$$

기준 \(H_0=67.4\) 대비 \(-0.227\%\)다.

더 중요한 것은 역방향 검산이다. 기준 \(H_0\)에서 \(\log S_{\rm dS}\)를 만들고 위 등식을 풀면

$$
N_e^{(H_0)}=57.19873204,
$$

반면 CE 원시 스펙트럼 쪽은

$$
N_e^{\rm CE}=57.19965162.
$$

차이는

$$
\Delta N_e=-0.00091958.
$$

이를 gauge count로 환산하면

$$
N_{\rm gauge}^{(H_0)}
=
\frac{2N_e^{(H_0)}}{3D_{\rm eff}}
=11.99980708.
$$

즉 late-time horizon entropy가 거의 정확히 원래의 \(N_{\rm gauge}=12\)를 되돌려준다.

민감도도 크다.

$$
H_0\propto
\exp\left[-\frac{\pi^2}{4}N_e\right],
$$

따라서 \(N_e\)가 하나 바뀌면

$$
\exp(-\pi^2/4)=0.08480497
$$

만큼 \(H_0\)가 바뀐다. 그러므로 이 일치는 느슨한 맞춤이 아니라 날카로운 경계 조건이다.

현재 해석은 다음과 같다.

$$
\boxed{
\text{late-time horizon이 primordial phase-area count를 읽는 이유는, 둘이 같은 경계 불변량 }I_H\simeq I_{\rm phase}\text{의 양끝이기 때문이다.}
}
$$

하지만 이는 아직 `Bridge`다. \(H_0\) 기준값을 외부에서 넣고 있으며, 독립 \(H_0\) 데이터와 BAO/CMB 조합에 대한 반례 검사가 필요하다.

---

### H0 branch falsification

위 invariant를 \(H_0\)의 예측식으로 뒤집으면

$$
\boxed{
H_0^{\rm pred}=67.247245\,{\rm km\,s^{-1}Mpc^{-1}}
}
$$

가 된다. 이 값은 단순히 기준 \(H_0=67.4\)를 다시 쓴 것이 아니라,

$$
\log S_{\rm dS}(H_0)+\pi\delta\sigma
\simeq
\frac{\pi^2}{2}N_e
$$

를 \(H_0\)에 대해 푼 결과다.

반증 게이트의 핵심 결과는 다음과 같다.

| branch | 대표 데이터 | CE pull | 의미 |
|---|---|---:|---|
| low \(H_0\) | Planck 2018 base LCDM \(67.4\pm0.5\) | -0.31 | 통과 |
| intermediate \(H_0\) | DESI-like BAO \(68.51\pm0.58\) | -2.18 | 약한 긴장 |
| lower/intermediate local ladder | CCHP 2025 JAGB/TRGB \(67.80{-}70.39\) | -0.20 to -1.62 | 통과 |
| high Cepheid/SN | SH0ES \(73.04\pm1.04\), JWST \(73.17\pm0.86\) | -5.57 to -6.89 | 반증 |

따라서 이 식의 물리적 의미는 "허블 텐션을 자동으로 필터링한다"가 아니다. 더 정확히는

$$
\boxed{
\text{late horizon invariant는 낮은 }H_0\text{ branch를 고른다.}
}
$$

이다. 만약 high Cepheid/SN branch가 최종적으로 맞다면, 현재의 단일 boundary invariant는 틀렸거나 late-time branch correction을 추가로 가져야 한다.

개선된 점은 세 가지다.

1. \(H_0\)를 외부 입력이 아니라 예측량으로 바꾸었다.
2. 성공 조건뿐 아니라 실패 조건을 만들었다.
3. 남은 문제가 철학 문제가 아니라 데이터 branch 문제임을 분리했다.

### High branch correction 후보

반증 게이트만 보면 high Cepheid/SN branch는 현재 식을 기각한다. 하지만 필요한 보정량을 역산하면 흥미로운 구조가 나온다.

low branch 식은

$$
\log S_{\rm low}
=
\frac{\pi^2}{2}N_e-\pi\delta\sigma
$$

이고, 관측 \(H_0\)가 요구하는 추가 correction은

$$
\Delta\log S_{\rm req}
=
-2\log\left(\frac{H_0^{\rm obs}}{H_0^{\rm low}}\right).
$$

SH0ES HST 값 \(73.04\)는

$$
\Delta\log S_{\rm req}=-0.16526238
$$

을 요구하고, SH0ES JWST update \(73.17\)은

$$
\Delta\log S_{\rm req}=-0.16881891
$$

을 요구한다. 그런데 CE 결핍 밀도는

$$
\delta\sigma=0.16911106.
$$

즉 high branch는 거의 정확히

$$
\Delta\log S_{\rm late}=-\delta\sigma
$$

로 열린다. 그러면

$$
\boxed{
\log S_{\rm high}
=
\frac{\pi^2}{2}N_e-\pi\delta\sigma-\delta\sigma
}
$$

이고

$$
\boxed{
H_0^{\rm high}=73.180689\,{\rm km\,s^{-1}Mpc^{-1}}.
}
$$

이것의 의미는 다음이다.

| 읽기 | 결핍 항 | 예측 |
|---|---|---:|
| global horizon | integrated defect \(\pi\delta\sigma\) | \(H_0=67.247245\) |
| local endpoint | integrated defect + local defect \((\pi+1)\delta\sigma\) | \(H_0=73.180689\) |

따라서 high \(H_0\)는 "아무 보정이나 넣으면 맞는다"가 아니라, 이미 있던 결핍 밀도 \(\delta\sigma\)를 적분하지 않은 local endpoint term으로 한 번 더 읽을 때 나온다.

하지만 이 후보는 아직 닫힌 설명이 아니다. 이유는 단순하다.

1. Planck/CMB horizon readout은 local endpoint term을 쓰면 망가진다.
2. SH0ES Cepheid/SN ladder는 local endpoint term을 쓰면 살아난다.
3. 그러므로 남은 핵심 문제는 \(H_0\) 값 자체가 아니라, 어떤 관측 채널이 어떤 boundary readout을 쓰는지다.

현재 판정:

$$
\boxed{
\text{high branch correction은 자연스러운 후보지만, readout 선택 원리가 없으므로 아직 Bridge/Open이다.}
}
$$

### Readout selector \(q\)

low/high branch를 하나의 식으로 묶으면

$$
\boxed{
\log S(q)
=
\frac{\pi^2}{2}N_e-\pi\delta\sigma-q\delta\sigma
}
$$

이다. 이때

$$
q=0
$$

은 CMB처럼 전체 horizon boundary를 읽는 global readout이고,

$$
q=1
$$

은 Cepheid/SN 거리사다리처럼 local endpoint defect까지 읽는 readout이다.

관측값에서 필요한 \(q\)를 역산하면

$$
q_{\rm req}
=
\frac{\log S_{\rm global}-\log S_{\rm obs}}{\delta\sigma}.
$$

`examples/physics/h0_readout/h0_readout_selector_gate.py`의 결과는 다음이다.

| channel | \(q_{\rm req}\) |
|---|---:|
| Planck/CMB | 0.0268 |
| DESI BAO+BBN | 0.2200 |
| CCHP JAGB | 0.0968 |
| CCHP TRGB JWST-only | 0.2717 |
| CCHP TRGB HST+JWST | 0.5402 |
| SH0ES HST Cepheids/SNe | 0.9772 |
| SH0ES JWST update | 0.9983 |

따라서 \(q\)는 단순히 "새 자유상수"가 아니라 관측 채널의 경계-국소성 정도를 나타내는 후보량으로 볼 수 있다.

$$
\boxed{
q\simeq0:\text{global horizon},\qquad
q\simeq1:\text{local endpoint}.
}
$$

중간값은 standard ruler/standard candle/anchor 혼합도가 endpoint defect를 부분적으로 읽는 경우로 해석할 수 있다.

하지만 여기에도 위험이 있다. 현재 \(q\)의 채널 기대값은 독립적인 광전파 방정식에서 유도한 것이 아니라, 데이터가 요구하는 순서를 보고 세운 selector hypothesis다. 그러므로 이 부분의 지위는

$$
\boxed{
\text{Bridge/Open test}
}
$$

이다. 다음 유도 목표는 \(q\)를 거리 측정 방식에서 직접 계산하는 것이다.

### Channel topology로 보는 \(q\)

selector를 더 수학적으로 압축하면 \(q\)는 관측 채널의 endpoint-locality 비율이다.

$$
\boxed{
q_{\rm topo}
=
\frac{L}{L+G}
}
$$

여기서

| 기호 | 의미 |
|---|---|
| \(L\) | local endpoint closure weight |
| \(G\) | global ruler/horizon closure weight |

이다. 이 식은 다음 boundary readout을 뜻한다.

$$
\log S
=
\frac{\pi^2}{2}N_e-\pi\delta\sigma
-
\frac{L}{L+G}\delta\sigma.
$$

즉 전체 horizon을 읽으면 \(L=0\)이고, endpoint를 직접 닫으면 \(G=0\)이다.

`examples/physics/h0_readout/h0_channel_topology_gate.py`의 최소 정수/유리 topology 비교:

| channel | \(L:G\) | \(q_{\rm topo}\) | \(q_{\rm req}\) |
|---|---:|---:|---:|
| CMB acoustic horizon | \(0:1\) | 0.0000 | 0.0268 |
| BAO+BBN inverse ladder | \(1:3\) | 0.2500 | 0.2200 |
| JAGB population endpoint | \(1:9\) | 0.1000 | 0.0968 |
| TRGB JWST-only | \(1:3\) | 0.2500 | 0.2717 |
| TRGB HST+JWST | \(1:1\) | 0.5000 | 0.5402 |
| Cepheid/SN local ladder | \(1:0\) | 1.0000 | 0.9772-0.9983 |

이 결과의 의미는 크다. \(q\)를 완전 자유상수로 두면 이론적 의미가 약하다. 하지만 \(q=L/(L+G)\)로 쓰면, \(q\)는 관측 방식의 topology가 된다.

다만 여기서도 미해결점은 남는다.

1. \(L,G\)를 아직 엄밀한 likelihood/covariance에서 계산하지 않았다.
2. JAGB의 \(G=9\), BAO/TRGB의 \(G=3\)은 population/global averaging의 최소 유리 모델이지 정리된 정리는 아니다.
3. 새 채널에 대해 \(H_0\)를 보기 전에 \(L,G\)를 먼저 선언해야 진짜 반증 가능하다.

그래서 현재 지위는

$$
\boxed{
\text{selector hypothesis upgraded: free }q \to q_{\rm topo}=L/(L+G),\quad \text{still Bridge/Open test.}
}
$$

### 외부채널 prospective test

selector가 진짜 의미 있으려면 \(H_0\) 값을 본 뒤 \(q\)를 맞추면 안 된다. 먼저 관측 채널의 topology를 정하고, 그 다음 \(H_0\)를 예측해야 한다.

`examples/physics/h0_readout/h0_prospective_channel_gate.py`는 이 기준으로 만든 첫 번째 외부채널 테스트다.

| channel | topology | \(q\) | \(H_0^{\rm pred}\) | 대표값 | 판정 |
|---|---:|---:|---:|---:|---|
| H0LiCOW/TDCOSMO power-law lens | \(L:G=1:0\) | 1.0000 | 73.181 | \(73.3\pm1.75\) | 통과 |
| TDCOSMO+SLACS hierarchical lens | \(1:3\) | 0.2500 | 68.684 | \(67.4\pm3.65\) | 통과 |
| Megamaser Cosmology Project | \(1:0\) | 1.0000 | 73.181 | \(73.9\pm3.0\) | 통과 |
| GW170817 bright siren | \(1:1\) | 0.5000 | 70.151 | \(70.3\pm5.15\) | 통과 |
| O4a dark+bright sirens | \(1:1\) | 0.5000 | 70.151 | \(68.0\pm4.1\) | 통과 |

합산하면

$$
\chi^2/{\rm dof}=0.462/5.
$$

핵심 해석은 다음이다.

1. local geometric closure는 \(q\simeq1\)이므로 high \(H_0\)를 예측한다.
2. global/hierarchical prior가 붙으면 \(q\)가 낮아져 low/intermediate \(H_0\)로 이동한다.
3. standard siren은 절대 거리와 host/catalog redshift가 함께 들어가므로 \(q\simeq1/2\)의 중간값을 예측한다.

따라서 \(q_{\rm topo}\)는 단순히 Cepheid와 CMB만 잇는 장치가 아니라, lens/megamaser/GW 채널까지 같은 축에 올린다.

현재 가장 정직한 말:

$$
\boxed{
\text{외부채널까지 통과했지만, }L,G\text{의 자동 유도는 아직 남았다.}
}
$$

### Covariance graph selector

손배정 \(L,G\)를 줄이기 위한 다음 형태는 graph conductance다. 관측 채널을 graph

$$
\mathcal G=(V,E)
$$

로 두고, 관측량 노드 \(o\)에서 local endpoint 집합 \(V_L\), global prior 집합 \(V_G\)로 가는 경로들을 센다.

각 edge에 reliability \(r_e\)를 주면, 경로 \(p\)의 conductance는

$$
C(p)=\frac{\prod_{e\in p}r_e}{|p|}
$$

이다. 그러면

$$
C_L=\sum_{p:o\to V_L}C(p),
\qquad
C_G=\sum_{p:o\to V_G}C(p),
$$

그리고

$$
\boxed{
q_{\rm graph}
=
\frac{C_L}{C_L+C_G}.
}
$$

`examples/physics/h0_readout/h0_covariance_graph_selector_gate.py`의 결과는

$$
\chi^2/{\rm dof}=0.402/10
$$

이다. 이 값은 기존 \(q_{\rm topo}\) 결과와 거의 같고, CMB/BAO/JAGB/TRGB/Cepheid/lens/megamaser/GW를 같은 규칙으로 읽는다.

따라서 \(q\)의 계층은 다음처럼 올라간다.

| 단계 | 식 | 지위 |
|---|---|---|
| free readout | \(q\) | 너무 자유로움 |
| topology ratio | \(q=L/(L+G)\) | `Bridge` |
| graph selector | \(q=C_L/(C_L+C_G)\) | 더 강한 `Bridge/Open test` |

남은 완전 유도는 실제 covariance matrix에서 \(r_e\)를 정하는 것이다. 자연스러운 후보는

$$
r_{ij}\propto |(C^{-1})_{ij}|
$$

또는 정규화된 Fisher edge

$$
r_{ij}
=
\frac{|F_{ij}|}{\sqrt{F_{ii}F_{jj}}}
$$

이다. 이 단계가 닫히면 \(q\)는 관측 채널마다 직접 계산되는 readout law가 된다.

### Fisher-edge robustness

실제 covariance를 넣기 전 확인해야 할 것은 안정성이다. \(q_{\rm graph}\)가 edge reliability의 작은 변화에 너무 민감하면, selector는 물리량이 아니라 graph 그리는 방식의 산물이 된다.

`examples/physics/h0_readout/h0_fisher_edge_robustness_gate.py`에서는

$$
C_L\to C_L e^{\eta_L},
\qquad
C_G\to C_G e^{\eta_G},
\qquad
\eta\sim\mathcal N(0,0.25^2)
$$

로 20,000회 흔들었다.

대표 결과:

| class | 대표 \(q\) | \(H_0\) 안정성 |
|---|---:|---|
| pure global | 0 | 완전 고정 |
| weak endpoint | 0.1 | \(67.66{-}68.03\) |
| intermediate | 0.25 | \(68.34{-}69.10\) |
| mixed | 0.5 | \(69.63{-}70.67\) |
| pure local | 1 | 완전 고정 |

즉 selector는 endpoint/locality class를 보존한다. 중간 channel은 넓어지지만 branch 순서는 유지된다.

현재 지위:

$$
\boxed{
q_{\rm graph}\text{는 toy edge perturbation에 안정하다. 다음은 실제 Fisher edge다.}
}
$$

### Fisher matrix selector

마지막으로 selector를 Fisher matrix 형식으로 쓴다. 관측 likelihood 근처에서 parameter vector \(\theta_i\)에 대한 Fisher matrix를

$$
F_{ij}
=
-\left\langle
\frac{\partial^2\log\mathcal L}{\partial\theta_i\partial\theta_j}
\right\rangle
$$

라고 하자. 그러면 edge reliability는

$$
\boxed{
r_{ij}
=
\frac{|F_{ij}|}{\sqrt{F_{ii}F_{jj}}}
}
$$

로 정의한다. 이 값은 Fisher partial-correlation 크기와 같은 역할을 하며, 단위 재정의

$$
\theta_i\to a_i\theta_i
$$

에 대해 불변이다.

이 edge로 local endpoint 집합 \(V_L\), global prior 집합 \(V_G\)로 가는 conductance를 계산하면

$$
\boxed{
q_F
=
\frac{C_L(F)}{C_L(F)+C_G(F)}.
}
$$

그리고 최종 readout 식은

$$
\boxed{
\log S
=
\frac{\pi^2}{2}N_e-\pi\delta\sigma-q_F\delta\sigma.
}
$$

`examples/physics/h0_readout/h0_fisher_matrix_selector_gate.py`에서는 schematic Fisher matrix로 이 형태를 검산했다. 파라미터 스케일을 \(e^{-3}\)에서 \(e^3\)까지 무작위로 바꿔도

$$
\max |\Delta q_F|\sim10^{-16}
$$

수준이다. 즉 \(q_F\)는 단위 선택에 의존하지 않는다.

이제 이론의 현재 도달점은 다음이다.

$$
\boxed{
\text{Hubble tension}
\quad\to\quad
\text{readout topology}
\quad\to\quad
\text{Fisher endpoint conductance}
}
$$

완전히 닫히려면 공개 likelihood의 \(F\) 또는 covariance \(C\)를 넣어 \(q_F\)를 실제로 계산해야 한다.

### Data-facing IO form

`examples/physics/h0_readout/h0_fisher_matrix_io_gate.py`는 이론식을 실제 covariance/Fisher 파일과 연결하는 입구다. 입력은 JSON이다.

핵심 필드는 다음이다.

| field | 의미 |
|---|---|
| `nodes` | likelihood parameter/calibration graph의 노드 목록 |
| `observable` | \(H_0\) readout을 시작하는 관측 노드 |
| `local_nodes` | endpoint anchor 노드 집합 |
| `global_nodes` | ruler/horizon/prior 노드 집합 |
| `matrix_type` | `fisher` 또는 `covariance` |
| `matrix` | Fisher 또는 covariance matrix |

covariance가 주어지면

$$
F=C^{-1}
$$

로 변환한 뒤

$$
r_{ij}=\frac{|F_{ij}|}{\sqrt{F_{ii}F_{jj}}}
$$

를 계산한다. 그다음

$$
q_F=\frac{C_L(F)}{C_L(F)+C_G(F)}
$$

와

$$
H_0(q_F)
$$

를 출력한다.

이제 남은 일은 명확하다. Planck, DESI, SH0ES, CCHP, TDCOSMO, GW likelihood에서 실제 covariance block을 가져와 이 IO gate에 넣으면 된다.

### IO regression

Fisher 입력과 covariance 입력이 같은 결과를 주는지도 고정했다.

예제:

$$
F=
\begin{pmatrix}
1 & 0.2 & 0.2\\
0.2 & 1 & 0\\
0.2 & 0 & 1
\end{pmatrix}
$$

와

$$
C=F^{-1}
$$

를 각각 JSON으로 넣으면 둘 다

$$
C_L=C_G=0.2,
\qquad
q_F=0.5,
\qquad
H_0=70.151263
$$

을 준다. 회귀 검산은

```bash
python examples/physics/h0_readout/h0_fisher_io_regression_gate.py
```

에 둔다.

batch 실행은

```bash
python examples/physics/h0_readout/h0_fisher_io_batch_gate.py examples/physics/h0_readout/h0_fisher_io_examples
```

이다. 여러 channel JSON을 한 번에 읽어 \(q_F\), \(H_0^{\rm pred}\), pull, \(\chi^2\)를 출력한다. 이 배치 계층까지 생겼기 때문에 실제 공개 covariance를 넣는 작업은 이제 파일 변환 문제로 분리된다.

입력 검증은

```bash
python examples/physics/h0_readout/h0_fisher_io_validate_gate.py examples/physics/h0_readout/h0_fisher_io_examples
```

로 수행한다. validator는 node 누락, matrix 비대칭, 비정방행렬, 비유한 숫자, 양이 아닌 대각, singular covariance를 사전에 잡는다. 따라서 실제 데이터 실행 순서는 validate 후 batch다.

negative validation은

```bash
python examples/physics/h0_readout/h0_fisher_io_negative_gate.py
```

에 둔다. unknown node, nonsymmetric matrix, nonpositive diagonal, singular covariance 네 반례를 모두 잡는다. 즉 readout law가 틀린 입력 때문에 조용히 오염되는 것을 최소한 막는다.

CSV matrix를 JSON 스키마로 바꾸는 변환기도 추가했다.

```bash
python examples/physics/h0_readout/h0_covariance_csv_to_json.py \
  --name "channel name" \
  --nodes obs,local_anchor,global_prior \
  --observable obs \
  --local-nodes local_anchor \
  --global-nodes global_prior \
  --matrix-type fisher \
  --matrix-csv path/to/matrix.csv \
  --output path/to/channel.json
```

이제 실제 공개 covariance가 CSV나 텍스트 matrix로 주어지면, 변환 후 validate/batch에 바로 넣을 수 있다.

labelled CSV table도 지원한다.

```bash
python examples/physics/h0_readout/h0_covariance_labelled_csv_to_json.py \
  --name "channel name" \
  --observable obs \
  --local-nodes local_anchor \
  --global-nodes global_prior \
  --matrix-type covariance \
  --labelled-csv path/to/labelled_matrix.csv \
  --output path/to/channel.json
```

이 adapter는 첫 행/첫 열 label이 같은 순서인지 검사하고, header에서 `nodes`를 자동 추출한다.

전체 IO 회귀는

```bash
python examples/physics/h0_readout/h0_fisher_io_full_suite.py
```

로 묶었다. validate, negative validation, Fisher/covariance regression, batch table을 한 번에 돌린다. 실제 covariance를 추가할 때는 이 suite가 통과해야 한다.

데이터 출처 재현성을 위해 source manifest도 추가했다.

```bash
python examples/physics/h0_readout/h0_fisher_manifest_validate_gate.py
```

manifest는 channel file, source, source URL, matrix role, channel class를 기록한다. 실제 공개 데이터에서는 이 manifest가 없으면 결과를 채택하지 않는 것으로 둔다.

manifest negative test는

```bash
python examples/physics/h0_readout/h0_fisher_manifest_negative_gate.py
```

로 둔다. missing file, missing source URL, duplicate file, missing field를 모두 거부한다. 즉 실제 데이터 bundle은 matrix-level validation과 source-level validation을 동시에 통과해야 한다.

## 4. 암흑 부문의 의미

암흑 부문은

$$
\sigma=\Omega_\Lambda+\Omega_{DM}
$$

로 읽는다. 내부 분할 비율은

$$
R=\frac{\Omega_{DM}}{\Omega_\Lambda}=0.38062660.
$$

따라서

$$
\Omega_\Lambda=\frac{\sigma}{1+R}=0.68907356,
$$

$$
\Omega_{DM}=\frac{\sigma R}{1+R}=0.26227972.
$$

의미는 다음과 같다.

| 항목 | CE 의미 |
|---|---|
| \(\Omega_\Lambda\) | 암흑 부문 중 배경/진공 성분 |
| \(\Omega_{DM}\) | 암흑 부문 중 구조를 유지하는 요동 성분 |
| \(R\) | 진공 성분 대비 요동 성분의 비율 |

따라서 CE에서 암흑물질은 우선 특정 WIMP 입자가 아니라

$$
\boxed{
\text{클라루스장 진공 위의 집합적 요동 성분}
}
$$

으로 읽는다.

브리지 스칼라 후보는

$$
m_\phi=m_p\delta^2=29.64757\,{\rm MeV}
$$

이지만, 이는 우주 암흑물질 입자 질량으로 확정된 값이 아니다. 국소 입자 언어로 집합 요동을 표현할 때 생기는 `Bridge` 후보량이다.

---

## 5. \(A_s\)가 알려준 핵심: 총 응답과 잔차 구동력의 분리

정확한 총 민감도는

$$
\left|\frac{dx}{dD}\right|
=
\frac{x(1-x)}{1-Dx}.
$$

이를 \(A_s\) 진폭으로 직접 읽으면

$$
A_s^{\rm raw}=7.8353\times10^{-9}
$$

가 되어 관측 기준보다 \(+273.1\%\) 크다. 이 읽기는 실패한다.

반면 잔차 원천은

$$
Q_{\rm source}=x(1-x).
$$

\(d=0\to d=3\) 전이를 반주기 투영으로 읽으면

$$
\Pi_{1/2}=\frac{2}{\pi}.
$$

여기에 유효 공간/시공간 정규화를 붙이면

$$
\gamma_{\rm eff}=\frac{D_{\rm eff}}{D_{\rm eff}+1}=0.76063719,
$$

$$
Q_{\rm A3c}
=
\frac{2}{\pi}
\sigma^{D_{\rm eff}/(D_{\rm eff}+1)}
x(1-x)
=0.02836622.
$$

이때

$$
A_s^{\rm A3c}=2.1038\times10^{-9}
$$

로 내려가며, 기준값 \(2.10\times10^{-9}\) 대비 \(+0.18\%\)다.

따라서 후보 원리는 다음이다.

$$
\boxed{
\text{스칼라 전이 관측량은 총 고정점 응답이 아니라 투영된 잔차 구동력을 읽는다.}
}
$$

이 원리는 아직 닫힌 공리가 아니다. \(A_s\) 하나만 맞으면 후보에 머문다. running, tensor, large-angle anomaly에서도 같은 구조가 살아야 한다.

---

## 6. 관측량 읽기 분류

현재 수식들은 다음 다섯 층으로 나누어 읽는 것이 가장 안전하다.

| 층 | 읽는 양 | 대표 관측량 | 지위 |
|---|---|---|---|
| 고정점 상태량 | \(x,\sigma,R\) | \(\Omega_b,\Omega_\Lambda,\Omega_{DM},n_s\) | 강함 |
| 총 민감도 | \(|dx/dD|\), \(D x\) | 수축률, 안정성 | 수학 구조 |
| 투영 잔차 구동력 | \(Q_{\rm A3c}\) | \(A_s\), CMB 대각 후보 | `Open/Open test` |
| 동결 잔차 | 전이 중 멈춘 값 | \(\eta,T_{\rm CMB}\) | `Phenomenology` |
| 후기 추출 잔차 | 관측 추출값의 late-time 이동 | \(H_0,S_8,f\sigma_8\) | 부분 해결 |

이 표가 현재 우주론 수식의 핵심 의미 지도다.

---

## 7. 후보 원리 목록

### 후보 1: 잔차 읽기 원리

전이 스칼라 관측량은 총 민감도 대신 투영 잔차 구동력을 읽는다.

승격 조건:

- \(A_s\) 외의 스칼라 전이 관측량에서도 같은 \(Q_{\rm A3c}\) 구조가 반복된다.
- running과 tensor가 같은 \(N_e\) 계열로 관측 상한과 충돌하지 않는다.
- CMB large-angle anomaly에서 \(5{-}6\%\) 크기뿐 아니라 축/위상 패턴까지 나온다.

### 후보 2: \(d=0\) 항등 경계상

\(d=0\)은 무한 특이점이 아니라 재귀 엔트로피 0의 항등 경계상이다.

승격 조건:

- \(\mathcal S_{\mathcal R}\)를 FLRW 곡률 초기값으로 옮기는 매핑이 필요하다.
- horizon entropy 또는 reheating entropy와의 스케일 연결이 필요하다.
- 특이점 회피 조건이 에너지 조건과 충돌하지 않아야 한다.

### 후보 3: 암흑물질 집합 요동 정체

\(\Omega_{DM}\)은 암흑 부문 내부의 집합적 요동 성분이다.

승격 조건:

- 직접검출 비검출 조건을 정량 단면적으로 내야 한다.
- halo mass function, core/cusp, missing satellite에 대한 스케일 의존 성장식이 필요하다.
- \(29.65\) MeV 브리지 스칼라가 어떤 실험 채널에서만 나타나는지 구분해야 한다.

### 후보 4: large-angle CMB 잔차 그림자

A3c 잔차가 완전히 등방 평균되지 않으면 low-\(\ell\) CMB에 흔적을 남긴다.

현재 후보 크기:

$$
S_Q\simeq0.38{-}0.41,
\qquad
A_{\rm hemi}\simeq5.7{-}6.0\%.
$$

승격 조건:

- preferred axis가 유도되어야 한다.
- quadrupole-octupole phase correlation이 나와야 한다.
- full CMB likelihood 또는 최소한 mask-robust statistic이 필요하다.

---

## 8. 다음 진행 우선순위

가장 효율적인 다음 단계는 full CMB likelihood가 아니다. 그 작업은 외부 지도와 likelihood pipeline이 필요해 무겁다. 먼저 외부 데이터 없이 이론 내부에서 계산 가능한 게이트를 세우는 편이 낫다.

우선순위는 다음이다.

1. **running/tensor 관측상한 게이트**

$$
\alpha_{\rm spec}=-6.1128\times10^{-4},
\qquad
r_{\rm tensor}=0.00366771.
$$

목적: A3c가 \(A_s\)만 맞춘 장치인지, 원시 스펙트럼 전체의 읽기 원리인지 압박한다.

이 게이트는 다음 계산으로 시작한다.

```bash
python examples/physics/inflation_next_tests_gate.py
```

현재 비교 기준으로 Planck 2018 inflation 분석은

$$
\frac{dn_s}{d\ln k}
=
-0.0045\pm0.0067
$$

을 주며, BICEP/Keck 2018 + Planck + BAO 조합의 보수적 tensor 상한은

$$
r<0.032
$$

로 둘 수 있다. 이 비교에서 CE는

$$
\alpha_{\rm spec}^{\rm CE}=-6.1128\times10^{-4}
$$

이므로 Planck running 중심값 대비

$$
\frac{\alpha_{\rm spec}^{\rm CE}-(-0.0045)}{0.0067}
=+0.58\sigma
$$

이고,

$$
r_{\rm tensor}^{\rm CE}=0.00366771
$$

은 현재 상한의

$$
\frac{0.00366771}{0.032}=0.1146
$$

즉 약 \(11.5\%\)다.

또한 CE 내부에서는

$$
\boxed{
\frac{r_{\rm tensor}}{-\alpha_{\rm spec}}=6
}
$$

이라는 고정 관계가 생긴다. 따라서 다음 관측 테스트는 단순히 \(r\) 하나가 아니라, 작은 음의 running과 작은 양의 tensor가 같은 \(N_e\) 계열로 함께 남는지 보는 것이다.

판정: 현재 관측상한과 충돌하지 않는다. 하지만 검출이 아니므로 `Open test`다.

2. **halo/JWST 구조 형성 게이트**

목적: 암흑물질을 집합 요동으로 읽는 해석이 은하 형성 시점과 halo abundance에 어떤 변화를 주는지 계산한다.

이 게이트의 첫 검산은 다음 계산으로 둔다.

```bash
python examples/physics/high_z_structure_proxy_gate.py
```

여기서는 full halo mass function을 계산하지 않는다. 전이함수, 질량-광도 변환, baryonic efficiency를 넣기 전에, 배경 성장률과 \(\sigma_8\) 보정만으로 high-\(z\) rare halo가 자동 증가하는지 보는 최소 프록시다.

선형 진폭은

$$
\sigma_8(z)=\sigma_8(0)D(z)
$$

로 두고, \(D(0)=1\)로 정규화한다. 현재 \(f\sigma_8\) 게이트에서 쓴 보정값은

$$
\sigma_{8,\rm static}=0.78533433,
\qquad
\sigma_{8,H_0}=0.77074116.
$$

\(z=8,10,12,15\)에서 CE/LCDM 선형 진폭비는 거의 일정하다.

| 가지 | \(\sigma_8(z)/\sigma_{8,\Lambda{\rm CDM}}(z)\) |
|---|---:|
| CE static | 0.97126 |
| CE \(H_0\) branch | 0.93206 |

따라서 배경 성장만으로는 high-\(z\) 구조가 증가하지 않는다.

희귀 halo 꼬리 프록시로

$$
F(>\nu)\propto
\operatorname{erfc}\left(\frac{\nu}{\sqrt2}\right)
$$

를 쓰면, CE 진폭비 \(R_A\)에서 같은 질량스케일의 rarity는

$$
\nu_{\rm CE}=\frac{\nu_{\Lambda{\rm CDM}}}{R_A}
$$

가 된다. 따라서 꼬리비는

$$
\frac{F_{\rm CE}}{F_{\Lambda{\rm CDM}}}
=
\frac{
\operatorname{erfc}\left[\nu_{\Lambda{\rm CDM}}/(R_A\sqrt2)\right]
}{
\operatorname{erfc}\left[\nu_{\Lambda{\rm CDM}}/\sqrt2\right]
}.
$$

대표적으로 \(z=10\)에서

| 가지 | \(\nu=2\) | \(\nu=3\) | \(\nu=4\) |
|---|---:|---:|---:|
| CE static | 0.867600 | 0.744414 | 0.602352 |
| CE \(H_0\) branch | 0.700964 | 0.477133 | 0.280211 |

이다. 값이 모두 1보다 작으므로, 배경 성장만으로는 CE가 high-\(z\) rare halo를 더 많이 만들지 않는다.

따라서 이 부분의 개선은 명확하다. 기존의 "CE가 JWST high-\(z\) 과밀을 설명할 수 있다"는 문장은 아직 쓰면 안 된다. 더 정직한 후보는 다음 중 하나다.

1. 집합 응축 암흑물질이 작은 스케일에서 별도의 transfer function을 만든다.
2. A3c 잔차가 \(A_s\) 전체가 아니라 small-scale primordial power에도 scale-dependent correction을 준다.
3. baryonic efficiency 또는 feedback history가 \(\Lambda\)CDM과 다르게 읽힌다.

이 셋 중 하나가 수식으로 닫히기 전까지 JWST/halo 항목은 `Open`이다.

### 필요한 small-scale boost 역산

배경 성장만으로 high-\(z\) rare halo가 늘지 않는다면, 다음 질문은 "얼마나 큰 작은 스케일 진폭 보정이 필요한가"다. 이를 별도 requirement gate로 계산한다.

```bash
python examples/physics/small_scale_boost_requirement_gate.py
```

희귀 꼬리 프록시는 그대로

$$
F(>\nu)\propto
\operatorname{erfc}\left(\frac{\nu}{\sqrt2}\right)
$$

이고, 전체 진폭비를 \(R_{\rm tot}\)라 하면

$$
\nu_{\rm CE}=\frac{\nu_{\Lambda{\rm CDM}}}{R_{\rm tot}}.
$$

목표 꼬리비 \(T\)를 만들기 위한 \(R_{\rm tot}\)는

$$
\frac{
\operatorname{erfc}\left[\nu_{\Lambda{\rm CDM}}/(R_{\rm tot}\sqrt2)\right]
}{
\operatorname{erfc}\left[\nu_{\Lambda{\rm CDM}}/\sqrt2\right]
}
=T
$$

를 풀어 얻는다. CE 배경 진폭비가 \(R_{\rm bg}\)라면 필요한 추가 boost는

$$
B_{\rm req}=\frac{R_{\rm tot}}{R_{\rm bg}}
$$

이다.

정적 가지에서는

$$
R_{\rm bg}^{\rm static}=0.97125559
$$

이고, \(H_0\) 가지에서는

$$
R_{\rm bg}^{H_0}=0.93208112.
$$

대표 결과는 다음이다.

| 가지 | \(\nu\) | 목표 꼬리비 | 필요한 추가 boost |
|---|---:|---:|---:|
| CE static | 3 | 1x | +2.96% |
| CE static | 3 | 2x | +11.02% |
| CE static | 3 | 5x | +25.04% |
| CE static | 3 | 10x | +39.67% |
| CE \(H_0\) branch | 3 | 1x | +7.29% |
| CE \(H_0\) branch | 3 | 2x | +15.69% |
| CE \(H_0\) branch | 3 | 5x | +30.29% |
| CE \(H_0\) branch | 3 | 10x | +45.54% |

후보 크기와 비교하면

$$
1+Q_{\rm A3c}=1.02836622,
$$

$$
1+x(1-x)=1.04628022,
$$

$$
1+\delta=1.17775842.
$$

\(\nu=3\) 대표 꼬리에서 실제 효과는 다음과 같다.

| 가지/후보 | 전체 진폭비 | \(\nu=3\) 꼬리비 |
|---|---:|---:|
| CE static, no boost | 0.97125559 | 0.744414 |
| CE static, \(1+Q_{\rm A3c}\) | 0.99880644 | 0.988293 |
| CE static, \(1+x(1-x)\) | 1.01620551 | 1.168831 |
| CE static, \(1+\delta\) | 1.14390445 | 3.232189 |
| CE \(H_0\), no boost | 0.93208112 | 0.477133 |
| CE \(H_0\), \(1+Q_{\rm A3c}\) | 0.95852074 | 0.647870 |
| CE \(H_0\), \(1+x(1-x)\) | 0.97521804 | 0.776475 |
| CE \(H_0\), \(1+\delta\) | 1.09776639 | 2.325890 |

해석은 분명하다.

- \(Q_{\rm A3c}\) 크기만으로는 정적 가지를 LCDM 수준으로 거의 복구할 뿐, 강한 과밀을 만들지는 못한다.
- \(x(1-x)\) 크기는 약한 enhancement 후보가 될 수 있다.
- \(\delta\) 크기의 scale-dependent boost는 \(\nu=3{-}4\) rare tail에서 의미 있는 과밀을 만들 수 있다.

따라서 JWST/halo 후보 원리는 다음처럼 좁혀진다.

$$
\boxed{
T_{\rm CE}(k)
=
T_{\Lambda{\rm CDM}}(k)
\left[1+\delta\,W_{\rm cond}(k)\right]
}
$$

여기서 \(W_{\rm cond}(k)\)는 큰 스케일에서는 0에 가깝고, early halo seed 스케일에서만 켜지는 집합 응축 transfer window여야 한다. 이 창 함수가 없으면 \(\delta\) boost는 \(S_8\)와 CMB small-scale power를 망가뜨릴 수 있다.

판정: 수식 방향은 생겼지만 \(W_{\rm cond}(k)\)가 아직 없으므로 `Open`이다.

3. **preferred-axis 후보 게이트**

목적: CMB 대각 이상현상의 크기뿐 아니라 방향/위상 예측을 만든다.

현재 가장 좋은 다음 작업은 1번이다. 이유는 이미 \(N_e\), \(\alpha_{\rm spec}\), \(r_{\rm tensor}\)가 계산되어 있고, 추가 모형 가정이 가장 적기 때문이다.

---

## 8.5 A3c 중력 환경 읽기 원리

A3c의 남은 핵심은

$$
\sigma^{D_{\rm eff}/(D_{\rm eff}+1)}
$$

의 의미다. 이 지수는 관측값에 맞춘 임의 지수가 아니라, CE의 \(+1\) 중력 섹터에서 온 것으로 읽는다.

CE의 내부 재귀 가지는 \(D_{\rm eff}\)개의 게이지-물질 깊이를 가진다. 하지만 스칼라 우주론 요동은 내부 게이지량으로 직접 관측되지 않고, metric perturbation으로 관측된다. 따라서 관측 채널은 내부 \(D_{\rm eff}\)가 아니라 그 내부 가지를 담는

$$
D_{\rm eff}+1
$$

의 중력/시공간 환경이다.

그래서 결핍 측도는

$$
\boxed{
\sigma_{\rm env}
=
\sigma^{D_{\rm eff}/(D_{\rm eff}+1)}
}
$$

로 정규화된다. 이때 A3c 읽기는

$$
\boxed{
Q_{\rm A3c}
=
\frac{2}{\pi}
\sigma_{\rm env}
x(1-x)
}
$$

이다.

세 성분의 지위는 다음처럼 분리한다.

| 성분 | 식 | 지위 |
|---|---|---|
| 잔차 원천 | \(x(1-x)\) | \(r_{\mathcal R}\)의 \(\partial_D\) 원천이므로 닫힘 |
| 반주기 투영 | \(2/\pi\) | \(d=0\to d=3\) 전이 위상 readout 후보 |
| 중력 환경 정규화 | \(\sigma^{D/(D+1)}\) | \(+1\) 중력 환경 readout 후보 |

검산은 다음으로 고정한다.

```bash
python examples/physics/gravitational_environment_readout_gate.py
```

현재 수치:

| 읽기 | \(A_s\) | 판정 |
|---|---:|---|
| 총 고정점 민감도 \(|dx/dD|\) | \(7.8353\times10^{-9}\) | 기각 |
| 반주기 잔차 \((2/\pi)x(1-x)\) | \(2.2696\times10^{-9}\) | 근접 후보 |
| 유효 \(+1\) 중력 환경 \(Q_{\rm A3c}\) | \(2.1038\times10^{-9}\) | 강한 후보 |

따라서 A3c의 현재 증명 상태는 다음이다.

$$
\boxed{
\text{raw susceptibility rejection}
\Rightarrow
\text{residual-source readout}
\Rightarrow
\text{half-cycle projection}
\Rightarrow
\text{+1 gravitational environment normalization}
}
$$

아직 부족한 부분은 마지막 화살표의 반복성이다. 이 \(+1\) 환경 readout이 horizon entropy, FLRW scale map, 또는 CMB large-angle anomaly에서도 반복되면 `Selection candidate`에서 `Selection/Bridge`로 승격할 수 있다.

---

## 8.6 A3c 반복 검산: 원시 스펙트럼과 CMB 대각

중력 환경 readout을 하나의 보정식으로 끝내지 않으려면, 같은 투영 계수를 여러 관측 손잡이에 반복해야 한다. 현재 반복 가능한 최소 공통 계수는

$$
\boxed{
P_{\rm GER}
=
\frac{2}{\pi}
\sigma^{D_{\rm eff}/(D_{\rm eff}+1)}
}
$$

이다. 그러면

$$
\boxed{
Q_{\rm A3c}
=P_{\rm GER}x(1-x)
}
$$

이고, 이 하나의 \(P_{\rm GER}\)에서 다음 값들이 동시에 나온다.

| 대상 | 식 | 값 | 판정 |
|---|---|---:|---|
| 스칼라 진폭 source | \(Q_{\rm A3c}=P_{\rm GER}x(1-x)\) | \(0.02836622\) | \(A_s\) 후보 |
| \(A_s\) | \(\frac{Q_{\rm A3c}^2}{\sigma^2}\frac{x}{2\pi N_e^2}\) | \(2.1038\times10^{-9}\) | `Open candidate` |
| quadrupole power handle | \(P_{\rm GER}^2\) | \(0.37567493\) | `Open test` |
| hemispherical handle | \(2Q_{\rm A3c}/\sigma=2P_{\rm GER}x\) | \(0.05963341\) | `Open test` |

검산:

```bash
python examples/physics/a3c_readout_closure_gate.py
```

이 게이트의 의미는 제한적이지만 중요하다.

1. \(A_s\)에 쓴 계수 \(P_{\rm GER}\)가 CMB large-angle 손잡이에도 재사용된다.
2. quadrupole suppression 크기는 \(O(0.38)\), hemispherical contrast 크기는 \(O(6\%)\)로 나온다.
3. 하지만 preferred axis, multipole phase correlation, full CMB likelihood가 없으므로 CMB anomaly를 닫았다고 말하지 않는다.

따라서 현재 승격 상태는 다음이다.

$$
\boxed{
\text{A3c/GER}
=
\text{reused scalar-amplitude and large-angle amplitude handle}
}
$$

아직 닫히지 않은 것은

$$
\boxed{
\text{axis/likelihood map}
}
$$

이다.

Horizon entropy는 같은 \(+1\) 철학에 속하지만, 같은 \(D/(D+1)\) 결핍 지수를 쓰지는 않는다. 현재 horizon lift는 별도 phase-area bridge다.

$$
\boxed{
\log S_{\rm dS}
\simeq
\frac{\pi^2}{2}N_e
-
\pi\delta\sigma
}
$$

따라서 A3c 증명에 horizon entropy를 억지로 합치지 않는다. 구조적으로는 둘 다 \(+1\) 중력 환경 readout이지만, 하나는 결핍 측도 정규화이고 다른 하나는 phase-area entropy lift다.

---

## 8.7 Preferred-axis no-go: 스칼라 GER의 닫힌 범위

위 반복 검산은 A3c/GER이 amplitude handle로 재사용된다는 것을 보인다. 하지만 이것만으로 CMB 대각 이상현상의 preferred axis까지 닫히지는 않는다. 이유는 수학적으로 단순하다.

현재 GER 입력은 모두 SO(3) 스칼라다.

$$
\alpha_s,\quad
\delta,\quad
D_{\rm eff},\quad
x,\quad
\sigma,\quad
P_{\rm GER},\quad
Q_{\rm A3c}.
$$

스칼라들의 조합은 다시 스칼라다. 따라서 방향을 가진 단위벡터

$$
n_i
$$

또는 방향을 고르는 traceless tensor

$$
T_{ij}
$$

를 만들 수 없다. 이 사실을 별도 게이트로 고정한다.

```bash
python examples/physics/a3c_preferred_axis_no_go_gate.py
```

현재 판정:

| 항목 | 가능 여부 | 이유 |
|---|---|---|
| \(A_s\) 진폭 | 가능 | \(Q_{\rm A3c}\) 스칼라 source |
| quadrupole suppression 크기 | 가능 | \(P_{\rm GER}^2\) |
| hemispherical contrast 크기 | 가능 | \(2Q_{\rm A3c}/\sigma\) |
| preferred unit axis | 불가 | GER 입력이 전부 스칼라 |
| multipole phase map | 불가 | sky-basis 또는 boundary vector 없음 |
| full CMB likelihood | 불가 | CMB map/covariance 미투입 |

따라서 현재 A3c의 정확한 완성 범위는

$$
\boxed{
\text{scalar amplitude readout closed as candidate}
}
$$

이지,

$$
\boxed{
\text{CMB anomaly closed}
}
$$

가 아니다.

Preferred-axis까지 닫으려면 새 입력이 아니라 새 **브리지 객체**가 필요하다. 가능한 최소 브리지는 다음 셋 중 하나다.

1. early-late horizon gradient에서 오는 단위벡터 \(n_i\),
2. 비등방 재귀 결핍 흐름에서 오는 traceless tensor \(T_{ij}\),
3. 실제 CMB map/covariance에서 축을 추정하고, CE는 축이 아니라 고정 진폭 \(P_{\rm GER}\)만 사전등록하는 data-facing bridge.

이 no-go는 A3c의 약점이지만 동시에 보호장치다. 스칼라 이론이 축까지 만들었다고 과장하지 않고, 축은 별도 symmetry-breaking bridge가 필요하다고 분리한다.

---

## 8.8 조건부 axis bridge: 축이 주어졌을 때 CE가 닫는 것

Preferred-axis no-go는 축을 절대 다룰 수 없다는 말이 아니다. 의미는 더 좁다.

$$
\boxed{
\text{scalar GER alone cannot derive }n_i
}
$$

하지만 어떤 독립 브리지에서 단위축 \(n_i\)가 주어지면, CE는 large-angle modulation의 진폭과 quadrupole tensor의 정규화를 고정할 수 있다.

검산:

```bash
python examples/physics/a3c_conditional_axis_bridge_gate.py
python examples/physics/a3c_conditional_axis_bridge_gate.py --axis 1 1 1
```

공통 스칼라 진폭은

$$
P_{\rm GER}=0.61292326,
\qquad
Q_{\rm A3c}=0.02836622.
$$

따라서

$$
\boxed{
S_Q=P_{\rm GER}^2=0.37567493
}
$$

를 quadrupole power handle로 두고,

$$
\boxed{
A_H=\frac{2Q_{\rm A3c}}{\sigma}
=2P_{\rm GER}x
=0.05963341
}
$$

를 hemispherical modulation amplitude로 둔다.

축 \(n_i\)가 주어졌을 때 modulation vector는

$$
\boxed{
m_i=A_H n_i
}
$$

이고, quadrupole orientation tensor는

$$
\boxed{
T_{ij}
=
S_Q\left(n_i n_j-\frac{1}{3}\delta_{ij}\right)
}
$$

이다. 이 텐서는 자동으로

$$
\mathrm{Tr}\,T=0
$$

을 만족하고,

$$
\boxed{
\|T\|_F
=
S_Q\sqrt{\frac{2}{3}}
=0.30673729
}
$$

를 갖는다. 이 값은 축 방향을 \(z\)-축으로 잡든 \((1,1,1)\)로 잡든 변하지 않는다.

따라서 현재 large-angle CMB의 정확한 상태는 다음이다.

| 층 | CE가 닫는가 | 내용 |
|---|---|---|
| 스칼라 진폭 | 예, 후보 | \(P_{\rm GER},Q_{\rm A3c},S_Q,A_H\) |
| 축 방향 | 아니오 | \(n_i\) bridge 필요 |
| 축이 주어진 뒤 텐서 정규화 | 예, 조건부 | \(T_{ij}=S_Q(n_in_j-\delta_{ij}/3)\) |
| 실제 CMB likelihood | 아니오 | map/covariance 필요 |

즉 다음 완성 문제는 이제

$$
\boxed{
\text{derive or ingest }n_i
}
$$

로 좁혀졌다.

---

## 8.9 Data-facing axis ingest: 축을 유도하지 않고 받아올 때

축 \(n_i\)를 이론 내부에서 아직 유도하지 못했다면, 다음으로 정직한 방식은 관측축을 **ingest**하는 것이다. 이 경우 CE는 축을 맞추지 않는다. 대신 이미 사전등록된 진폭

$$
A_H=0.05963341
$$

과 tensor normalization

$$
\|T\|_F=0.30673729
$$

만 검산한다.

검산:

```bash
python examples/physics/a3c_cmb_axis_ingest_gate.py
```

현재 게이트는 두 대표 행을 둔다.

| 관측 행 | 축 | 관측 진폭 | CE 진폭 | 판정 |
|---|---:|---:|---:|---|
| Planck/PR3 temperature HPA representative | \((205^\circ,-20^\circ)\) | \(0.070\pm0.021\) | 0.05963341 | \(-0.49\sigma\) |
| Planck PR4 Sevem E-mode local variance | \((234^\circ,-14^\circ)\) | broad \(0.060{-}0.130\) | 0.05963341 | edge-compatible |

temperature와 E-mode 대표축의 각거리는

$$
28.33^\circ
$$

이다. 이 각도 자체를 CE가 예측한 것은 아니다. 현재 의미는 다음으로 제한한다.

$$
\boxed{
\text{observed }n_i\text{를 넣으면 CE 고정 진폭은 large-angle HPA 크기와 양립한다.}
}
$$

문헌 맥락은 다음과 같다. Planck 2018 isotropy paper는 large-angle temperature anomaly들이 이전 릴리스와 일관되게 남아 있음을 보고하지만, polarization에서 같은 anomaly를 확정적으로 검출한다고 주장하지 않는다. PR4 HPA 분석은 temperature 방향이 기존 보고와 잘 맞고, Sevem PR4 E-mode local-variance dipole에서 \((234^\circ,-14^\circ)\) 방향 및 2.8% p-value 수준의 결과를 보고한다. 따라서 이 gate는 discovery claim이 아니라 **data-facing compatibility bridge**다.

다음에 닫아야 할 것은 둘 중 하나다.

1. \(n_i\)를 early-late horizon gradient 또는 anisotropic defect flow에서 유도한다.
2. 실제 CMB map/covariance pipeline으로 \(n_i\)와 likelihood를 추정하되, CE의 \(A_H\)는 고정값으로 둔다.

---

## 8.10 Amplitude likelihood proxy: full likelihood 전 최소 검산

실제 CMB likelihood 전 단계로, 대표 HPA 진폭 행들을 단순 가우시안 행으로 묶어 common-amplitude proxy를 만들 수 있다. 이 검산은 map-level 분석이 아니며, CE 고정 진폭이 관측 진폭 스케일에서 크게 벗어나는지만 본다.

검산:

```bash
python examples/physics/a3c_cmb_likelihood_proxy_gate.py
```

입력 행은 위 ingest gate와 같은 representative temperature/E-mode amplitude를 사용한다.

| 행 | \(A_{\rm obs}\) | \(\sigma_A\) | CE pull |
|---|---:|---:|---:|
| Planck/PR3 temperature HPA representative | 0.070 | 0.021 | \(-0.49\sigma\) |
| Planck PR4 Sevem E-mode proxy | 0.090 | 0.035 | \(-0.87\sigma\) |

inverse-variance weighted common amplitude는

$$
A_{\rm fit}=0.07529412\pm0.01800735
$$

이고, CE 고정값

$$
A_{\rm CE}=A_H=0.05963341
$$

은

$$
\frac{A_{\rm CE}-A_{\rm fit}}{\sigma_{\rm fit}}
=-0.87\sigma
$$

이다.

단순 proxy \(\chi^2\)는

| 모델 | \(\chi^2\) | 비고 |
|---|---:|---|
| null \(A=0\) | 17.7234 | no-asymmetry |
| CE fixed \(A=A_H\) | 0.9964 | pre-registered amplitude |
| best-fit common \(A=A_{\rm fit}\) | 0.2401 | one fitted amplitude |

따라서

$$
\Delta\chi^2_{\rm CE-null}
=
-16.7269
$$

이고,

$$
\Delta\chi^2_{\rm CE-fit}
=
+0.7564.
$$

해석은 다음이다. CE fixed amplitude는 null보다 훨씬 낫고, one-parameter best-fit보다도 \(<1\) \(\chi^2\) 정도만 나쁘다. 하지만 이 수치는 대표 진폭 행의 proxy일 뿐이며, sky map, mask, covariance, multipole coupling을 포함하지 않는다.

따라서 현재 지위는

$$
\boxed{
\text{pre-likelihood amplitude compatibility}
}
$$

이다. full closure는 여전히

$$
\boxed{
\text{map/covariance likelihood with } A=A_{\rm CE}\text{ fixed}
}
$$

가 필요하다.

---

## 8.11 A3c/GER closure package 최종 판정

현재 A3c 증명 시도는 다음 게이트로 묶는다.

```bash
python examples/physics/a3c_closure_package_gate.py
```

이 gate는 A3c를 `Exact`로 승격하지 않는다. 대신 현 단계에서 닫힌 범위와 열린 범위를 분리한다.

| 층 | 결과 | 판정 |
|---|---:|---|
| raw scalar readout \(A_s[|dx/dD|]\) | \(7.8353\times10^{-9}\), \(+191.18\sigma\) | 기각 |
| GER scalar readout \(A_s[Q_{\rm A3c}]\) | \(2.1038\times10^{-9}\), \(+0.13\sigma\) | `Selection candidate` |
| common projection \(P_{\rm GER}\) | 0.61292326 | \(+1\) 중력 환경 후보 |
| quadrupole handle \(S_Q\) | 0.37567493 | amplitude handle |
| hemispherical handle \(A_H\) | 0.05963341 | amplitude handle |
| conditional tensor norm | 0.30673729 | \(n_i\) 조건부 닫힘 |
| HPA proxy \(\Delta\chi^2_{\rm CE-null}\) | \(-16.7269\) | pre-likelihood compatibility |
| HPA proxy CE-fit gap | \(+0.7564\) | best-fit 대비 작음 |

따라서 현재 승격 가능한 문장은 다음이다.

$$
\boxed{
\text{A3c/GER is a Selection-candidate scalar readout principle}
}
$$

그리고 더 좁은 data-facing 문장은 다음이다.

$$
\boxed{
\text{A3c/GER gives a pre-likelihood CMB large-angle amplitude bridge}
}
$$

승격하지 않는 문장도 명시한다.

$$
\boxed{
\text{A3c/GER is not yet an Exact theorem or full CMB anomaly closure}
}
$$

남은 blocker는 하나로 압축된다.

$$
\boxed{
\text{derive }n_i
\quad\text{or}\quad
\text{run map/covariance likelihood with }A=A_H\text{ fixed}
}
$$

반증 조건은 다음이다.

1. \(A_s\), running, tensor family가 같은 \(N_e/A3c\) 구조를 거부한다.
2. robust CMB HPA amplitude가 \(A_H=0.05963341\)에서 멀어진다.
3. full CMB likelihood에서 \(A=A_H\) fixed model이 mask/trials 보정 후 null보다 나쁘다.
4. 유도 또는 ingest된 \(n_i\)가 quadrupole/octupole phase test를 통과하지 못한다.

이로써 A3c 라운드는 값 맞춤이 아니라, 닫힌 범위와 열린 병목을 분리한 상태로 마무리한다.

---

## 8.12 자기참조재귀 추가 연구 audit

우주론에서 자기참조재귀를 더 쓸 여지는 남아 있다. 다만 그 위치는 core fixed point를 다시 흔드는 쪽이 아니라, 관측량이 어떤 잔차/경계층을 읽는지 고르는 readout/selector 층이다.

이를 `examples/physics/recursive_cosmology_research_audit.py`로 분리했다. 핵심 수치는 다음이다.

| 항목 | 값 |
|---|---:|
| fixed-point residual | \(-1.041\times10^{-16}\) |
| contraction \(D_{\rm eff}x\) | 0.15458752 |
| global horizon \(q=0\) | \(H_0=67.247245\) |
| endpoint \(q=1\) | \(H_0=73.180689\) |
| branch gap | \(5.933444\,{\rm km\,s^{-1}Mpc^{-1}}\) |

따라서 추가 재귀의 우선순위는 다음처럼 잡는다.

| 우선순위 | 재귀 레버 | 다음 gate |
|---:|---|---|
| 1 | \(H_0\) \(q\)-selector | `prospective_covariance_graph_selector_gate` |
| 1 | residual contraction cascade | `residual_cascade_invariant_gate` |
| 2 | \(A_s\)/A3c residual readout | `primitive_spectrum_common_readout_gate` |
| 2 | fixed-point kernel deformation | `kernel_deformation_no_free_parameter_gate` |
| 3 | \(d=0\) zero-residual boundary | `d0_measure_transport_gate` |
| 3 | horizon phase-area lift | `early_late_measure_preservation_gate` |

여기서 가장 강한 연구축은 \(H_0\) selector다. 이미 \(q=0\) global horizon readout은 낮은 \(H_0\) branch를, \(q=1\) endpoint readout은 높은 Cepheid/SN branch를 만든다. 그러므로 다음 검증은 \(H_0\) 값을 보고 \(q\)를 맞추는 것이 아니라, source/covariance graph만으로 \(q\)를 먼저 예측한 뒤 관측값과 비교하는 것이다.

금지선도 명확하다. \(d=0\)을 물리적 장소로 부르거나, 관측량마다 별도 재귀항을 붙이거나, selector 없이 high-\(H_0\) correction을 승격하면 안 된다. 현재 결론은 다음이다.

$$
\boxed{
\text{추가 자기참조재귀는 가능하지만, 새 fixed point가 아니라 readout/selector 재귀로 써야 한다.}
}
$$

### \(q\)-selector 자기참조 폐합

위 우선순위 1번을 바로 압박하기 위해 `examples/physics/h0_readout/h0_recursive_selector_self_reference_gate.py`를 추가했다. 이 gate는 세 지도를 분리한다.

$$
\text{source graph}\rightarrow q_{\rm graph},
\qquad
q_{\rm graph}\rightarrow H_{0,\rm pred},
\qquad
H_{0,\rm obs}\rightarrow q_{\rm obs}.
$$

\(q_{\rm graph}\rightarrow H_{0,\rm pred}\rightarrow q_{\rm back}\)은 항등적으로 닫히므로 증거로 세지 않는다. 진짜 검사는 \(H_0\) 값을 보지 않고 정한 \(q_{\rm graph}\)가 관측값에서 역산한 \(q_{\rm obs}\)와 맞는지다.

| channel | \(q_{\rm graph}\) | \(q_{\rm obs}\) | \(q\) pull | \(H_{0,\rm pred}\) | \(H_0\) pull |
|---|---:|---:|---:|---:|---:|
| Planck 2018 base LCDM | 0.0000 | 0.0268 | -0.31 | 67.247 | -0.31 |
| DESI DR2 BAO no-CMB calibration | 0.2500 | 0.2200 | +0.30 | 68.684 | +0.30 |
| CCHP 2025 JWST-only JAGB | 0.1000 | 0.0968 | +0.01 | 67.818 | +0.01 |
| CCHP 2025 TRGB HST+JWST | 0.5000 | 0.5402 | -0.12 | 70.151 | -0.12 |
| SH0ES JWST update | 1.0000 | 0.9983 | +0.01 | 73.181 | +0.01 |
| TDCOSMO+SLACS hierarchical lenses | 0.2500 | 0.0268 | +0.35 | 68.684 | +0.35 |
| Megamaser Cosmology Project | 1.0000 | 1.1157 | -0.24 | 73.181 | -0.24 |
| GW standard siren representative | 0.5000 | 0.5250 | -0.03 | 70.151 | -0.03 |

결과는

$$
\chi^2_q/{\rm dof}=0.379/8,
\qquad
\chi^2_{H_0}/{\rm dof}=0.381/8,
$$

이고, 항등 루프의 최대 drift는 \(1.349\times10^{-13}\)이다.

따라서 \(q\)-selector는 단순한 사후 보정이라기보다,

$$
\boxed{
\text{관측 채널의 source graph가 자기참조 readout branch를 먼저 고르는 규칙 후보}
}
$$

로 한 단계 올라간다. 아직 남은 병목은 toy graph edge를 실제 covariance/Fisher matrix에서 ingest하는 것이다.

### residual contraction cascade

두 번째 1순위였던 residual contraction cascade도 `examples/physics/residual_cascade_invariant_gate.py`로 분리했다. 질문은 단순하다.

$$
\text{raw로 실패한 항목을 관측량마다 다른 재귀로 고치는가,}
\quad\text{아니면 하나의 잔차 cascade로 접는가?}
$$

현재 살아남는 cascade는 다음이다.

$$
Q_{\rm total}
=
\frac{x(1-x)}{1-D_{\rm eff}x},
\qquad
Q_{\rm source}=x(1-x),
$$

$$
Q_{\rm phase}
=
\frac{2}{\pi}Q_{\rm source},
\qquad
Q_{\rm GER}
=
\frac{2}{\pi}\sigma^{D_{\rm eff}/(D_{\rm eff}+1)}Q_{\rm source}.
$$

계산 결과는 다음이다.

| 항목 | 값 | 판정 |
|---|---:|---|
| raw \(A_s\) pull | \(+191.18\sigma\) | 기각 |
| GER \(A_s\) pull | \(+0.13\sigma\) | selection candidate |
| \(Q_{\rm total}/Q_{\rm source}\) | 1.18285456 | raw 과증폭 원인 |
| \(P_{\rm GER}\) | 0.61292326 | 공통 projection |
| \(r_{\rm tensor}/(-\alpha_{\rm spec})\) | 6.00000000 | \(N_e\)-family lock |
| \(2Q_{\rm GER}/\sigma\) | 0.05963341 | large-angle handle |

따라서 여기서의 자기참조재귀는 새 보정항이 아니라,

$$
\boxed{
\text{total susceptibility를 버리고 source residual을 같은 projection cascade로 반복해서 읽는 규칙}
}
$$

이다. 이 규칙은 \(A_s\) 하나에서는 살아남고, large-angle amplitude handle까지 같은 \(P_{\rm GER}\)를 재사용한다. 하지만 running, tensor, CMB map/covariance likelihood가 같은 cascade를 거부하면 내려와야 한다.

### fixed-point kernel 변형 no-free-parameter audit

다음으로 core fixed point 자체에 자기참조재귀를 더 넣는 시도를 `examples/physics/kernel_deformation_no_free_parameter_gate.py`로 검사했다. 비교한 변형은 두 가지다.

$$
x=\exp[-cD_{\rm eff}(1-x)],
$$

그리고

$$
x=\exp[-D_{\rm eff}(1-x)-\kappa(1-x)^2].
$$

둘 다 \(c\)나 \(\kappa\)를 \(\Omega_b\)에 맞추면 \(x\)를 관측 중심값에 정확히 놓을 수 있다. 하지만 이 경우 새 파라미터 하나를 쓴 것이므로, \(A_s\) GER readout과 함께 보면 전체 점수가 오히려 나빠진다.

| model | \(c\) | \(\kappa\) | fitted params | \(\Omega_b\) pull | \(n_s\) pull | \(A_s^{\rm GER}\) pull | \(\chi^2\) | AIC |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| CE minimal kernel | 1.00000000 | 0.00000000 | 0 | -1.63 | +0.03 | +0.13 | 2.684 | 2.684 |
| \(c\) tuned to \(\Omega_b\) | 0.99627164 | 0.00000000 | 1 | -0.00 | +0.03 | +2.91 | 8.493 | 10.493 |
| \(\kappa\) tuned to \(\Omega_b\) | 1.00000000 | -0.01246222 | 1 | -0.00 | +0.03 | +2.91 | 8.493 | 10.493 |

따라서 kernel 쪽 결론은 보수적이다.

$$
\boxed{
\text{core kernel에 자기참조재귀를 더 넣는 것은, 독립 유도 없이는 현재 승격하지 않는다.}
}
$$

현재 더 강한 길은 kernel 변형이 아니라 readout/selector 층이다. kernel 변형은 \(c\)나 \(\kappa\)가 관측값을 보기 전에 다른 원리에서 고정될 때만 다시 열 수 있다.

### \(d=0\) measure transport gate

남은 \(d=0\) 경계 축은 `examples/physics/d0_measure_transport_gate.py`로 다시 정리했다. 여기서는 \(d=0\)을 물리적 장소나 도착 상태로 쓰지 않는다. 허용하는 것은 오직 다음 transport다.

$$
x_0=1,\qquad \sigma_0=0,\qquad S_{\mathcal R,0}=0,
$$

그리고 \(d=3\) 수축 가지에서

$$
S_{\mathcal R}
=
-\log x
=
D_{\rm eff}(1-x).
$$

계산 결과는 다음이다.

| transport | 값 | 판정 |
|---|---:|---|
| \(S_{\mathcal R}-D_{\rm eff}(1-x)\) | \(+2.220\times10^{-15}\) | entropy identity closed |
| contraction \(k=D_{\rm eff}x\) | 0.15458752 | stable forward branch |
| reverse amplification \(1/k\) | 6.46882737 | \(d=0\) 도착 금지 |
| \(10^{-60}\) residual까지 필요한 step | 74.00 | finite residual erasure count |
| \(Q_{\rm source}=x(1-x)\) | 0.04628022 | residual source |
| \(Q_{\rm phase}=(2/\pi)Q_{\rm source}\) | 0.02946290 | half-cycle projection |
| \(Q_{\rm GER}\) | 0.02836622 | selection candidate |
| \(\exp(-2N_e)\) | \(2.07498\times10^{-50}\) | flatness direction |
| \(k^{N_e}\) | \(4.17888\times10^{-47}\) | residual suppression |

따라서 \(d=0\)의 현재 승격 문장은 다음으로 제한한다.

$$
\boxed{
d=0\text{은 physical place가 아니라 zero-measure boundary이며, }
d=3\text{ branch의 entropy/residual transport로만 읽는다.}
}
$$

아직 열려 있는 것은 절대 horizon entropy, reheating entropy, FLRW curvature 초기값으로 가는 scale lift다. 즉 \(d=0\)은 우주론의 형식적 시작점을 넘어서긴 했지만, 완전한 초기상태 정리는 아니다.

### early-late measure preservation

horizon phase-area lift도 `examples/physics/early_late_measure_preservation_gate.py`로 \(H_0\) selector와 합쳤다. 보존량은 다음처럼 쓴다.

$$
I_{\rm phase}
=
\frac{\pi^2}{2}N_e,
$$

그리고 late readout은 채널별 \(q\)를 포함해

$$
I_{\rm late}
=
\log S_{\rm dS}
+\pi\delta\sigma
+q\delta\sigma.
$$

여기서 \(q=0\)은 global horizon readout, \(q=1\)은 local endpoint-defect readout이다. \(q\)는 \(H_0\)를 본 뒤 맞추는 값이 아니라 source graph에서 먼저 정한다.

핵심 수치는 다음이다.

| 항목 | 값 |
|---|---:|
| \(\pi^2/2\) | 4.93480220 |
| \(d=3\) adjoint phase measure | 4.93480220 |
| \(I_{\rm phase}\) | 282.26896669 |
| invariant \(\chi^2/{\rm dof}\) | 0.379/8 |

채널별 결과는 \(q\)-selector self-reference gate와 같은 pull 구조를 갖는다. 해석만 다르다. 이전 gate는 \(q_{\rm graph}\)와 \(q_{\rm obs}\)의 일치를 보았고, 이번 gate는 그 일치가

$$
I_{\rm late}\simeq I_{\rm phase}
$$

라는 early-late measure preservation으로 다시 읽힌다는 것을 보인다.

따라서 horizon lift의 현재 승격 문장은 다음이다.

$$
\boxed{
\text{late horizon entropy readout is a channel-corrected Bridge to the primordial phase measure}
}
$$

단, 여전히 `Exact`는 아니다. 남은 병목은 toy source graph가 아니라 실제 covariance/Fisher edge를 ingest하고, late horizon entropy가 이 phase measure를 읽는 동역학적 이유를 유도하는 것이다.

### 자기참조재귀 package 판정

마지막으로 `examples/physics/self_recursive_cosmology_package_gate.py`에서 위 결과를 하나의 승격표로 묶었다.

| lever | layer | status | 핵심 증거 | 다음 gate |
|---|---|---|---|---|
| minimal fixed-point kernel | core kernel | Closed/minimal | fixed-point residual \(-1.041\times10^{-16}\), tuned AIC \(10.493\) > minimal AIC \(2.684\) | kernel derivation only |
| \(d=0\) measure transport | boundary | Boundary principle | entropy identity error \(+2.220\times10^{-15}\), contraction \(0.15458752\) | FLRW/reheating/horizon scale lift |
| residual cascade | readout | Selection candidate | raw \(A_s\) \(+191.18\sigma\), GER \(A_s\) \(+0.13\sigma\) | joint primitive-spectrum/CMB likelihood |
| \(H_0\) \(q\)-selector | selector | Channel-corrected Bridge | \(q\)-space \(\chi^2/{\rm dof}=0.379/8\) | real covariance/Fisher edge ingest |
| early-late phase measure | horizon bridge | Channel-corrected Bridge | \(\pi^2/2=(2\pi)^2/(3^2-1)\), invariant \(\chi^2/{\rm dof}=0.379/8\) | late horizon readout dynamics |

따라서 전체 package 판정은 다음이다.

$$
\boxed{
\text{self-recursive cosmology is a Selection/Bridge package, not an Exact theorem.}
}
$$

안전한 자기참조재귀는 이제 세 층에만 둔다.

1. readout/selector 층: 관측 채널이 어떤 잔차/경계를 읽는지 먼저 정한다.
2. measure transport 층: \(d=0\)을 physical place가 아니라 zero-measure boundary로 쓴다.
3. early-late bridge 층: late horizon readout을 channel-corrected primordial phase measure로 되돌린다.

반대로 core kernel 변형은 독립 유도 전까지 보류한다.

재현성은 `examples/physics/self_recursive_cosmology_closure_suite.py`로 묶는다. 현재 suite는 다음 17개 gate를 실행한다.

| suite gate | 역할 |
|---|---|
| `recursive_cosmology_research_audit.py` | 전체 자기참조재귀 후보 분류 |
| `h0_recursive_selector_self_reference_gate.py` | \(q_{\rm graph}\) vs \(q_{\rm obs}\) 자기참조 폐합 |
| `residual_cascade_invariant_gate.py` | \(A_s\)/large-angle residual cascade |
| `kernel_deformation_no_free_parameter_gate.py` | core kernel 변형 보류 |
| `d0_measure_transport_gate.py` | \(d=0\) zero-measure boundary transport |
| `early_late_measure_preservation_gate.py` | channel-corrected phase measure preservation |
| `self_recursive_cosmology_package_gate.py` | 최종 Selection/Bridge package 판정 |
| `h0_fisher_manifest_validate_gate.py` | Fisher/covariance bundle manifest 검증 |
| `h0_fisher_io_validate_gate.py` | channel JSON schema/ingest 검증 |
| `h0_fisher_io_regression_gate.py` | Fisher와 covariance 입력 동등성 회귀 |
| `h0_fisher_io_batch_gate.py` | bundle 전체 \(q_F,H_0\) batch 검증 |
| `h0_fisher_real_readiness_gate.py` | synthetic IO와 real covariance data-boundary 분리 |
| `h0_real_covariance_requirements_gate.py` | real covariance 승격 blocking requirement 정의 |
| `h0_real_covariance_promotion_decision_gate.py` | manifest channel별 real-ready/not-promoted 승격 판정 |
| `self_recursive_cosmology_prediction_ledger_gate.py` | 다음 예측, decisive check, falsifier ledger |
| `self_recursive_cosmology_claim_audit_gate.py` | 문서 claim의 gate coverage와 limitation 검증 |
| `self_recursive_cosmology_artifact_index_gate.py` | 필수 script/report/example/doc/suite 참조 검증 |

현재 suite 결과는 `17/17` 통과다. 단, `h0_fisher_real_readiness_gate.py` 기준 real-ready covariance channel은 현재 `0`개다. 따라서 \(q\)-selector는 Fisher/covariance IO path까지는 닫혔지만, real covariance closure는 아직 data-boundary다.

real covariance promotion의 blocking requirement는 public source URL/DOI, pinned version, machine-readable covariance/Fisher 또는 posterior covariance, observable/local/global node labels, H0 비교 전 role map, positive-definite 또는 invertible covariance다. 최종 scalar \(H_0\) 값만으로는 승격하지 않는다.

현재 synthetic Fisher 예제들은 IO path 검증에는 쓰지만 `h0_real_covariance_promotion_decision_gate.py`에서 모두 `not-promoted`로 판정한다. 즉 예제 bundle은 readout plumbing 회귀 테스트이지 real covariance evidence가 아니다.

prediction ledger 기준 다음 반증 가능 축은 real BAO+SN covariance, real ladder covariance, primitive-spectrum joint likelihood, CMB large-angle fixed-amplitude likelihood, FLRW scale lift, late horizon readout dynamics, kernel no-free-parameter derivation이다.

claim audit 기준 허용 claim은 `claimable-minimal`, `claimable-boundary`, `selection-candidate`, `bridge`, `limitation`, `package-status`로 제한한다. 즉 `Exact` claim은 현재 허용하지 않는다.

---

## 9. 한 줄 결론

$$
\boxed{
\text{CE 우주론의 핵심은 값 맞춤이 아니라, 관측량마다 어떤 재귀 성분을 읽는지 분류하는 것이다.}
}
$$

닫힌 것은 고정점 상태량이다. 가장 강한 후보는 투영 잔차 구동력이다. 남은 문제는 이 후보가 \(A_s\) 하나를 넘어 원시 스펙트럼, 구조 형성, CMB 대각 이상현상까지 반복되는지 검증하는 것이다.

