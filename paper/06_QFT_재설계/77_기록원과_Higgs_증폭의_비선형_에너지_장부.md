# 77. 기록원과 Higgs 증폭의 비선형 에너지 장부

## 77.1 계산 전 등록: CE-UR4-D3

**[후보: 같은 작용의 자율적인 비선형 증폭]** 76장의 선형 Higgs
섭동을 실제 고전장 좌표로 되돌리고 기록원과 함께 진화시킨다.
배경에 외력·감쇠·열원이나 새 검출기 결합을 더하지 않는다.
고전적 증폭 seed를 양자 진공 fluctuation 또는 실제 확정 사건으로
동일시하지 않는다.

1. 원래 82차원 작용에서 $\Sigma_Y,\Sigma_3,X_{\alpha Y},
   X_{\alpha3},q$의 다섯 복소 좌표를 추출한다.
   $H_4=q/\sqrt2$, $\bar H_4=-q/\sqrt2$이고 다른 Higgs 성분은 0이다.
   두 Cartan 방향과 한 기록 질량 종류의 불변성, 정준 운동항,
   $D=0$ 및 gauge current 0을 전체 tensor와 독립 다항식으로 확인한다.
2. 기존 $V=g_5=\lambda_\Sigma=\lambda_R=\lambda_H=1$,
   $m_0=10^{-4}$를 유지한다. 공통 기준 $m_{\rm ref}=m_-$,
   $\xi_{\rm ref}=0.1$,
   $n_0=2\xi_{\rm ref}^2m_{\rm ref}^2/\kappa$로 고정한다.
   두 기록 종류 모두 $A_\alpha=\sqrt{n_0/(2m_\alpha)}$,
   $S(0)=A_\alpha e^{i\theta}$, $\dot S(0)=-im_\alpha S(0)$를 쓴다.
   이는 같은 선도 입자 수 밀도의 정규화이며 질량별 새 피팅이 아니다.
3. Higgs seed는 공통 $q(0)=\eta A_{\rm ref}$, $\dot q(0)=0$다.
   $\eta=0.01,0.1$ 및 $\theta=0,\pi/4$의 네 준비점을 두 질량에서
   모두 계산한다. seed 0의 불변 해도 대조한다.
   무거운 세 좌표의 초기값은 light 좌표를 고정한 potential의
   국소 정상점 중 원래 진공에 연결된 것으로 정하고, 초기 속도는
   그 해의 접공간에서 light 속도를 따른다. 이후에는 이 좌표들도
   완전한 동역학으로 진화하며 매 순간 다시 최소화하지 않는다.
4. 동일한 물리적 구간 $m_{\rm ref}t\in[0,60]$에서 전체
   $\rho=\sum_i(|\dot z_i|^2+|F_i|^2)$와
   $p=\sum_i(|\dot z_i|^2-|F_i|^2)$를 계산한다.
   $F_i$ 좌표별로 정의한 양의 부분 에너지의 반대칭 교환 current와
   전체 에너지 보존을 독립 검산한다. 이 분할을 유일한 상호작용
   에너지 배분이나 독립 입자 에너지라고 부르지 않는다.
5. 사전 지정 pointer 진단은 $|q|^2/A_{\rm ref}^2=1$의 첫 통과,
   마지막 구간 $m_{\rm ref}t\in[50,60]$에서 그 문턱의 유지·재통과다.
   peak, late-window 최소·최대와 Higgs 부분 에너지 비율을 모두 기록한다.
   원래 기록의 자유 운동식 잔차와 heavy record 성분도 추적한다.
   source phase나 seed를 골라 실패를 숨기지 않는다.
6. 기호·tensor·gauge·current 허용치는 정규화된 $10^{-8}$,
   에너지 보존과 독립 적분 비교는 $10^{-6}$를 우선 요구한다.
   필요한 수치 가속은 별도 실행 환경에서 사용하고 원래 모델 코드는
   바꾸지 않는다. 시간 간격 수렴과 다른 적분 방법으로 확인하며,
   오차가 이를 만족하지 않으면 정밀한 pointer 판정을 유보한다.

닫힌 균일 증폭기가 기록을 유지하지 못하면 그 적용 범위를 축소해
보존한다. 다음 준비·경계 분기는 유한 source와 밖으로 나가는 방사장을
같은 상태공간에 유지하는 D4로 둔다. 밖으로 나간 진폭·에너지를
삭제하거나 임의 감쇠항으로 대신하지 않는다는 조건을 먼저 고정한다.
이번 균일 계산으로 그 공간적 장치를 검증했다고 주장하지 않는다.

사전 등록을 해시로 고정한 뒤 수치 계산한다. 새 관측 입력·피팅은 없고
scientific_success=false, full_joint_rmse=null을 유지한다.

## 77.2 검산 결과

**[유도·독립 구현 검산]** 아래 다섯 장의 부분공간은 원래 고전
bosonic 작용 아래 닫힌다. 횡방향 양자 요동의 안정성이나 전체
양자 instrument까지 닫혔다는 뜻은 아니다. 사전 등록 SHA256은
`e9b621150bfc4b1a0d71e6054a038ecb71eb95ce0dedae8d10588e400ab72133`이다.

좌표를 $(z,t,s,u,q)=(\Sigma_Y,\Sigma_3,X_{\alpha Y},X_{\alpha3},q)$,
$a=1/(2\sqrt{15})$, $b=3a$, $d=1/2$로 둔다.
원래 진공에서 이동한 superpotential은 고정된 $V=1$ 단위에서

$$
W=-\frac{z^2}{2}-\frac{5t^2}{2}
  +\frac{m s^2}{2}+\frac{(m-4)u^2}{2}
  +\frac{a z^3}{3}+3azt^2+azs^2+3azu^2+6atsu
  -\frac{(bz+dt)q^2}{2}.
$$

여기서 $m$은 기존 $m_+$ 또는 $m_-$다. $V$를 복원하면 이차항의
계수는 질량 차원 1, 장은 차원 1, $W$는 차원 3이다. 세제곱 결합은
무차원이다. 음의 holomorphic 이차계수를 음의 물리적 질량제곱으로
바꾸어 읽지 않는다.

$$
\begin{aligned}
F_z&=-z+az^2+3at^2+as^2+3au^2-bq^2/2,\\
F_t&=-5t+6azt+6asu-dq^2/2,\\
F_s&=(m+2az)s+6atu,\\
F_u&=(m-4+6az)u+6ats,\\
F_q&=-(bz+dt)q,\\
\ddot z_i&=-\sum_j W_{ji}^*F_j.
\end{aligned}
$$

전체 82성분 좌표로 가는 정준 embedding은 실수이며
$\mathcal B^\dagger\mathcal B=I_5$다. 두 Cartan adjoint와 같은 크기의
$H_4=-\bar H_4$가 $D=0$을 만든다. 일반 복소 좌표와 속도에서도
전체 gauge current가 0이고, $F$와 그 gradient가 embedding 밖으로
나가지 않음을 직접 검사했다. $u,t$는 seed가 자라면 반응하므로
이를 0으로 고정한 두 singlet 계산을 사용하지 않았다.

검산은 원래 82성분 tensor의 진공 이동식, 위 다항식의 SymPy 미분,
별도 작성한 빠른 운동식, potential 유한차분을 대조한다. 정준 운동항,
불변성, gauge current, 에너지 교환과 초기 정상점용 실수 Hessian을
포함한 최대 정규화 오차는 $4.912\times10^{-13}$이다.

## 77.3 전체 보존량과 부분 에너지 교환

**[정의·유도]** 균일 고전장과 $A_\mu=0$에서

$$
E_i=|\dot z_i|^2+|F_i|^2,\qquad
\rho=\sum_iE_i,\qquad
p=\sum_i\bigl(|\dot z_i|^2-|F_i|^2\bigr).
$$

각 $E_i$는 비음수지만 상호작용을 $F_i$ index에 배정한 분할이다.
독립 입자 에너지나 유일한 sector stress라고 부르지 않는다.
그 시간 변화는

$$
\dot E_i=\sum_j J_{ij},\qquad
J_{ij}=2\operatorname{Re}\!left(
F_i^*W_{ij}\dot z_j-F_j^*W_{ji}\dot z_i\right),\qquad
J_{ji}=-J_{ij}
$$

이므로 $\dot\rho=0$이다. $J$는 자연단위에서 차원 5,
$\rho,p$는 차원 4다. 압력은 따로 보존되는 수가 아니다.
$(z,t)$, $(s,u)$, $q$의 부분 에너지 사이에 교환이 일어나도
전체 에너지는 동일한 국소 작용의 시간 병진 대칭으로 보존된다.
이 계산은 공급된 평탄 배경의 stress 장부이며 Einstein 방정식이나
공통 중력 작용을 새로 유도한 것이 아니다.

초기 heavy 좌표는 light 좌표를 고정한 potential 정상점을 진공에서
연속 추적해 정한다. 실수 heavy/light Hessian을 $H_{HH},H_{HL}$로
쓰면 초기 속도는

$$
v_H=-H_{HH}^{-1}H_{HL}v_L
$$

이다. 이것은 초기 조건에만 사용한다. 적분 중에는 heavy 좌표를
다시 최소화하지 않고 위의 다섯 운동식을 그대로 푼다.
질량별 에너지 밀도는 같은 선도 수 밀도에서도 약 $m_\alpha n_0$로
다르다. 이를 맞추려고 seed나 결합을 재조정하지 않는다.

## 77.4 공통 준비 조건의 비선형 결과

**[수치 산출]** 공통 준비량은

$$
n_0=4.70377513293\times10^{-8}V^3,\qquad
A_{\rm ref}=0.00828155018484V,\qquad
t_{\rm end}=174967.735130/V
$$

이다. $n_0$는 초기 자유 입자 해석의 정규화다. 이 상호작용계에서
새로 보존되는 연속 입자 수를 증명했다고 읽지 않는다.
두 질량·두 seed·두 초기 위상 모두 같은 물리적 시간과 같은 문턱으로
비교했다. $Q=|q|^2/A_{\rm ref}^2$, $\tau=m_{\rm ref}t$로 쓰면
등록된 여덟 경우는 다음과 같다.

| 질량 | $\eta$ | $\theta$ | $Q$ peak | 첫 상향 통과 $\tau$ | 첫 하향 통과 $\tau$ | $\tau\in[50,60]$의 $Q$ 최소–최대 |
|---|---:|---:|---:|---:|---:|---:|
| $m_-$ | 0.01 | 0 | 6.98854 | 37.4740 | 56.2793 | 0.36705–4.91774 |
| $m_-$ | 0.01 | $\pi/4$ | 7.03923 | 37.6180 | 56.4812 | 0.38918–5.10473 |
| $m_-$ | 0.1 | 0 | 7.06460 | 21.1845 | 39.9833 | 0.03930–0.07550 |
| $m_-$ | 0.1 | $\pi/4$ | 7.09640 | 21.2468 | 40.0748 | 0.02329–0.06808 |
| $m_+$ | 0.01 | 0 | 7.07875 | 37.4780 | 56.1822 | 0.34279–4.94795 |
| $m_+$ | 0.01 | $\pi/4$ | 7.07360 | 37.5128 | 56.2226 | 0.34738–4.98076 |
| $m_+$ | 0.1 | 0 | 7.07026 | 21.1804 | 39.9156 | 0.01153–0.06313 |
| $m_+$ | 0.1 | $\pi/4$ | 7.06659 | 21.2015 | 39.9419 | 0.01154–0.06370 |

출력 격자는 $\Delta\tau=0.01$의 6001점이다. peak·최솟값·최댓값은
이 표본에 대한 값이고, 문턱 시간은 인접 표본의 선형 보간이다.
연속 시간의 엄밀한 극값 오차 한계를 증명한 표는 아니다. 그래도
모든 경우에서 문턱 1을 충분히 넘은 뒤 충분히 아래로 돌아오는
수치 반례가 나타나며, 마지막 구간 전체의 문턱 유지 검사는 모두 실패한다.
관측 창이나 seed를 다시 골라 이 실패를 지우지 않았다.

Higgs의 $F_i$ 분할 에너지 비율 최대치는 $m_+$에서 3.924–3.958%,
$m_-$에서 7.290–8.048%다. 원래 기록장의 자유 운동식 잔차

$$
\frac{|\ddot s+m_\alpha^2s|}{m_\alpha^2A_\alpha}
$$

의 최대치는 각각 0.0821–0.0881과 0.1686–0.1859다. 이는 주어진 자유
운동식에서 벗어난 정도이며 시간 의존 배경의 물리적 pole mass 이동률은
아니다. heavy record의 $|u|/A_\alpha$ 최대치는
$5.26\times10^{-6}$–$6.42\times10^{-6}$다. 작은 heavy 진폭도
정준 작용과 에너지 교환에 포함한 상태에서 얻은 결과다.

$q(0)=\dot q(0)=0$에서는 정확히 $q(t)=0$인 불변 해가 존재한다.
그러나 그 고전 해는 76장의 양자 진공 공분산과 다른 초기 상태다.
이를 진공에서 실제 검출 사건이 생기지 않는다는 증명으로 사용하지 않는다.

## 77.5 오차·실패 기록과 재현

**[구현 검증]** DOP853의 상대/절대 허용치
$(2\times10^{-8},2\times10^{-11})$와
$(2\times10^{-10},2\times10^{-13})$를 먼저 비교했다. 적분 좌표는
$z_i/A_{\rm ref}$와 $\dot z_i/(m_{\rm ref}A_{\rm ref})$다.
오차 제어가 내부 시간 간격을 결정하며 위의 출력 격자는 내부 적분 간격이
아니다. 두 질량의 $\eta=0.01,\theta=0$에서는 속도 비교가 각각
$1.2523\times10^{-5}$와 $2.8401\times10^{-6}$여서 등록된
$10^{-6}$ 수치 문턱을 통과하지 못했다.

이 첫 실패를 원본 JSON에 남기고 그 두 경우만
$(2\times10^{-12},2\times10^{-15})$까지 다시 세밀하게 계산했다.
기존 fine 실행과의 속도 차이는 각각 $1.0440\times10^{-8}$,
$1.0288\times10^{-8}$로 줄었다. 장·속도·$Q$·부분 에너지·전체 보존을
모두 포함한 최종 여덟 경우의 최대 비교 오차는 $6.260\times10^{-7}$로
허용치 안이다. 전체 에너지 상대 drift는 $5.840\times10^{-14}$ 이하,
$Q$의 수렴 차이는 $6.04\times10^{-13}$ 이하다.

독립 적분 방법 RK45는 코드에서 계산 전에 고정한 준비점
$m_-,\eta=0.1,\theta=0$의
$\tau\in[0,1],[30,31],[59,60]$ 세 구간에서 대조했다.
같은 시작 상태에서 모든 장과 속도의 최대 차이는
$9.449\times10^{-10}$이다. 이는 세 구간의 독립 방법 검산이며
RK45로 여덟 전체 궤적을 다시 적분했다고 확대하지 않는다.
initial heavy 정상점·접공간 검사와 전체 tensor의 에너지·전류 검산도
각각 등록된 허용치를 통과했다.

- [원래 다섯 장 계산](../../verify/ce_record_backreaction.py), [첫 실행 결과](../../verify/ce_record_backreaction.json)
- [실패한 두 경우의 추가 수렴 계산](../../verify/ce_record_backreaction_refinement.py), [최종 추가 검산](../../verify/ce_record_backreaction_refinement.json)

각 결과에 사전 등록과 실행 코드의 SHA256을 저장했다. 추가 결과는
첫 결과 파일의 SHA256까지 확인한다. 첫 실행 파일은 두 수치 실패 때문에
종료 코드 1을 내는 기록을 보존하고, 추가 수렴 실행은 최종 통과 시 0을 낸다.
이를 물리 반례와 혼동하거나 첫 실행의 실패 표시를 사후에 삭제하지 않는다.
공유 모델과 프로젝트 의존성 파일을 바꾸지 않은 별도 환경의 재현 명령은
다음과 같다. 첫 명령의 기록을 만든 다음 둘째 명령을 별도로 실행한다.

```powershell
uv run --no-project --python 3.11.15 --link-mode copy --with numba==0.67.0 --with numpy==2.4.6 --with scipy==1.17.1 --with sympy==1.14.0 python -X utf8 -B -u verify/ce_record_backreaction.py
uv run --no-project --python 3.11.15 --link-mode copy --with numba==0.67.0 --with numpy==2.4.6 --with scipy==1.17.1 --with sympy==1.14.0 python -X utf8 -B -u verify/ce_record_backreaction_refinement.py
```

실행 환경은 Python 3.11.15, NumPy 2.4.6, SciPy 1.17.1,
SymPy 1.14.0, Numba 0.67.0이다.

## 77.6 판정과 다음 경계 가정 D4

**[적용 범위 축소·반례]** 같은 작용에서 고전적 비선형 증폭과 전체 에너지
장부는 계산된다. 그러나 등록된 공통 준비에서 닫힌 균일 D3를 유지되는
pointer로 사용하는 가정은 여덟 경우 모두 실패한다. 이 결과는 CE-UR4
전체의 기각이나 모든 닫힌 Hamiltonian 기록장치의 불가능 정리가 아니다.
새 위상·seed·피팅으로 이번 실패를 덮지 않는다.

**[다음 가정: 미검증]** D4는 유한 원천과 밖으로 전파되는 장을 같은
상태공간에 유지한다. 균일 근사를 풀고 같은 다섯 장 작용의 공간 gradient를
포함하면 국소 에너지 관계는

$$
\rho=\sum_i\left(|\dot z_i|^2+|\nabla z_i|^2+|F_i|^2\right),\qquad
\mathbf j_E=-2\operatorname{Re}\sum_i\dot z_i^*\nabla z_i,\qquad
\partial_t\rho+\nabla\cdot\mathbf j_E=0
$$

이다. 원천 내부의 에너지 감소는 바깥 장의 에너지 증가와 함께 계산해야
한다. 바깥 진폭을 삭제하는 흡수항이나 임의 마찰로 이를 대신하지 않는다.
유한 초기 원천의 실제 준비, 공간·시간 격자 수렴, 외곽 경계의 되돌아오는
신호, 가시·비가시 방사 성분과 기록 유지 조건을 먼저 고정해야 한다.
방사 에너지가 바깥으로 간다는 사실만으로 영구 pointer나 확정 사건을
얻었다고 선언하지 않는다. 이 절은 다음 준비·경계 분기의 조건이며 공간
장치에 대한 수치 검증은 아니다.

**[열린 주장]** 공급된 고전 seed의 양자 상태와 확률 법칙, 실제 비동기
관측 instrument, 중력의 공통 유도, 관측량의 고정된 공동 예측은 아직
없다. 새 관측 입력이나 보정 피팅을 사용하지 않았으며
scientific_success=false, full_joint_rmse=null이다. 이 수치 검산은
VAL-01의 관측 개선이나 OBJ-01–04의 달성으로 승격하지 않는다.
