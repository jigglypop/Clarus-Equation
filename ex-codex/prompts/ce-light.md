$ce-research 스킬로 light 모드 run을 수행하라. 선행 run의 후속·반복 작업이다.

대상: $ARGUMENTS

1. 계약에 `PREDECESSOR: _workspace/ce/<선행-run>`을 반드시 적는다.
2. 레인은 math-verifier만 실행한다. 선행 run이 감사한 결론은 재유도하지 않고 경로만 인용한다.
3. 감사(gate)와 40-final-report.md는 유지한다.
4. 마지막 메시지에 `CE_RUN=...` 한 줄을 남긴다.
