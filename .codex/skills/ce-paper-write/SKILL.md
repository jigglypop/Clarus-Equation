---
name: ce-paper-write
description: CE 강의·유도·독자 가이드와 한국어 물리·수학 논문 원고를 핵심 기여, 독자, 정리 의존 관계와 재현 가능한 유도 중심으로 작성하거나 개정한다. 서사와 가독성을 고칠 때 사용한다. 주장 원장·상수 원장·진리값 표의 지위 갱신에는 사용하지 않는다.
---

# CE 논문 작성

논문 작성자는 안정화된 원장의 주장과 지위를 읽어 독자가 따라갈 수 있는 논증으로 바꾼다. 원장의 지위를 새로 판정하거나 수정하지 않는다.

## 형식 규약

- 안정화된 원장의 `[정의]`, `[정리]`, `[공리]`, `[산출]`, `[경험식]`, `[미완성]`, `[예측]` 지위를 강화하거나 섞지 않는다.
- 완전한 반례로 폐기된 부모 주장을 원고에서 제거하고, 살아남는 좁은 명제와 반례 범위를 함께 밝힌다.
- 외부 입력은 원출처와 판본을 인용하고, 관측 정합을 증명처럼 쓰지 않는다.
- 블록 수식은 `$$ ... $$`, 인라인 수식은 `$...$`를 사용한다.
- 한국어 문장은 번역투와 불필요한 피동을 줄이고, 처음 등장하는 기호를 사용 전에 정의한다.

## 소유 경계

- 강의, 유도, 논문 원고, 독자 가이드만 편집한다.
- 원장은 수정하지 않는다.
- 원장과 원고를 함께 다루면 안정화된 원장을 읽기 전용 입력으로 사용한다.
- 원장의 지위·기호·수치가 논문에 맞지 않아 보여도 직접 고치지 않는다. 충돌 위치와 필요한 원장 수정을 보고한다.
- 같은 작업자가 원장과 원고를 오가며 동시에 편집하지 않는다.

## 정본 위치

- 상세 논문 정본은 주제에 맞는 `docs/<분야>/<논문>/` 폴더에 둔다. `00_논문목차.md`가 제목·초록·핵심 결론과 `## 논문 조립 순서`를 제공하고, 그 아래에서 역할이 겹치지 않는 장별 Markdown을 읽는 순서대로 연결한다.
- 기존 주제는 관련 장을 제자리 갱신한다. 새 장은 기존 장과 독립된 목적이 있을 때만 추가하며 `_new`, `v2`, `final` 사본을 만들지 않는다.
- 짧은 강의·유도·독자 가이드는 기존 분야 파일을 갱신할 수 있다. 한 파일이 여러 독립 절을 반복하거나 논문 길이가 되면 같은 입구+장 구조로 분할한다.
- `_workspace/`와 run의 `artifacts/`에는 논문 사본을 두지 않는다. run의 `40-final-report.md`는 `DOCS_PAPER: docs/<분야>/<논문>/00_논문목차.md`와 결론·증거 경로만 적는 인계 기록이다.
- `docs/README.md`와 분야 읽기 지도에는 목차 입구만 한 번 연결하고 장 본문을 복제하지 않는다.

## 개요 게이트

초안 요청에는 본문보다 먼저 아래 개요를 고정한다. 사용자가 완성 원고를 바로
요청했으면 이 개요를 내부 작업 기준으로 삼고 확인을 기다리지 말고 집필한다.

1. 핵심 기여를 한두 문장으로 고정한다.
2. 전제 독자를 "이 주제를 1년 전에 처음 배우던 저자"로 적는다.
3. 문서 유형을 독립 논문, 강의, 유도, 독자 가이드 중 하나로 정한다.
4. 정의 → 보조정리 → 핵심 정리 → 따름정리의 의존 관계를 적는다.
5. 각 절의 목적과 앞 절에서 넘겨받는 결과를 한 문장씩 적는다.
6. 공리, 외부 입력, 근사, 경계조건, 미완성 다리를 본문보다 먼저 목록화한다.

## 구조

독립 수학 논문은 제목 → 초록 → 서론 → 본론 → 남은 문제 → 참고문헌 순으로 쓴다. 별도 결론 절은 두지 않는다. 서론 앞부분에 핵심 결과를 제시하고 표준 정의는 필요한 자리로 미룬다.

실증·뇌 논문은 제목 → 초록 → 서론·연구 질문 → 자료와 코호트 → 측정·전처리·QC → 사전 고정 분석 → 결과 → control·sensitivity·음성 결과 → 논의 → 한계·반증 조건 → 재현성·자료/코드 → 참고문헌 순으로 쓴다. 전체 사전 고정 endpoint를 표로 보존하고, 주 결과의 개별 기여도와 강건성 진단, reference·window·estimand 의존성을 산문으로 해석한다. 검산표와 상태 로그만으로 원고를 대신하지 않는다.

강의·유도·독자 가이드는 독립 논문의 절 이름을 억지로 복제하지 않는다. 대신 첫 두 문단에서 무엇을 보이는지, 왜 필요한지, 어떤 순서로 진행하는지 밝히고 각 절을 목적 문장으로 시작한다.

## 논증과 문장

- 뇌/AGI 논문형 문서는 `.codex/harnesses/real_brain_equation_discovery_loop.md`에 따라 확립된 생물 기전식·측정모형과 저자의 CE 추가항 경계를 먼저 밝히고, 사전 고정 실데이터 시험, 실패·잔차, 대안 모델과 판본 개정을 숨기지 않는다. simulator 성과는 L0로 분리하며 생물학적 결론을 대체하지 않는다.
- 실증 원고는 독자가 원장을 열지 않아도 표본·판본·단위·estimand·분모·QC·불확도·핵심 수치와 한계를 재구성할 만큼 상세히 쓴다. 계산 성공과 과학적 증명, 기술 bootstrap과 모집단 추론, 같은 자료의 사후 재분석과 독립 확인을 명시적으로 구분한다.
- 정의를 사용하는 정리보다 앞에 둔다. 정리, 보조정리, 따름정리를 구분한다.
- 본문 증명은 "증명."으로 시작해 `□`로 끝낸다. 정본 증명 원장을 링크하는 문서는 링크의 범위와 필요한 전제를 한 문장으로 요약한다.
- 유도는 한 줄에 한 단계씩 쓰고 적용한 정의·가정·법칙을 말로 밝힌다. 조건, 근사와 경계조건을 생략하지 않는다.
- 직관은 "직관적으로는" 또는 "참고(Remark)"로 표시하고 증명과 섞지 않는다.
- 지위 태그를 문단의 표지로만 쓰고 태그 블록을 연속 나열하지 않는다. 태그 사이에는 앞 결과와 다음 목적을 잇는 산문을 둔다.
- 저자의 독자적 제안은 "본 논문에서 제안하는"이라고 밝힌다. 논증 공백은 문장으로 메우지 않고 원장 작성자에게 돌려보낸다.
- 문장을 기호나 숫자로 시작하지 않고, 산문의 논리 기호는 말로 푼다. 한 문단에는 한 아이디어만 둔다.

## 탈고 게이트

처음 읽는 독자의 눈으로 처음부터 다시 읽고 기호의 첫 등장, 정의 사용 순서, 절 사이 연결, 종이와 펜 재현 가능성, 번역투를 검사한다. 원장 표처럼 읽히는 강의·유도 원고는 완성으로 보고하지 않는다.
