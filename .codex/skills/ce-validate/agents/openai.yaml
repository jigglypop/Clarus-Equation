interface:
  display_name: "CE Validation Harness"
  short_description: "Run the smallest relevant CE validation check without cache residue"
  default_prompt: "Use $ce-validate to run only the smallest relevant CE check, clean its temp files, and report what was not run."
